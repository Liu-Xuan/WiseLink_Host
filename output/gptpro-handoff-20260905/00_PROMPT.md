你是 WiseLink 项目的独立架构评审与产品设计顾问。请基于我提供的材料和可访问的代码，对当前工作思路、业务语义、上下文管理、智能体运行方式、交互复核、技术架构和实施顺序进行深入评估，给出能直接交给开发主控执行的改进方案。

本轮只分析和提出修订稿，不操作生产、不提交业务动作、不替我修改仓库或发布。不要只复述或赞同现有方案，也不要因为代码已经这样写就把现状当成合理约束。

一、我的产品目标与开发偏好

WiseLink 面向航空工程文件与工程事项的辅助评估。先把工程文件信息按业务语义组织起来，再结合有解释价值的关联文档具体片段、知识空间 RAG 返回的背景，以及工程师补充的信息，管理一个持续演进的“上下文评估包”。

工程师选择文件和评估对象后，智能体应理解问题、按照 Job-Aid 等评估方法及评估点开展分析，按需要利用持续丰富的 Skill、MCP、知识空间和其他授权信息能力调查，并形成分项判断与综合评估。

我明确要求优先让项目实际运行，在真实运行与使用反馈中迭代。不要增加很多防御性编程、gate、断言、冻结契约、baseline 或 hash，使项目仍然不能使用。默认复用 Git、版本号、主键、事务/原子操作、唯一约束、类型和普通测试。只有说明具体失败场景及现有机制为什么不足，才建议额外机制。保留已有认证、权限、数据安全、并发更新与正式业务确认；不静默失败，不将缺少可选背景变成整个流程不可运行。

二、需要准确理解的业务语义

关联资料不是越多越好，也不是所有引用都要导入。选择的是“能帮助回答当前工程问题的片段”。

例如，纯施工步骤的 AMM 工卡往往不能帮助理解问题机理，不应自动装入机理分析背景；但评估涉及施工条件、工具、接近、停场或维修要求时，可以按需读取相关内容。不要按文件类型一刀切，也不要只凭同 ATA、关键词相近或检索排名认定关系。

以我描述的 Boeing 问题链为例：前期航司问答邮件可能提供问题信号；FTD 可能跟踪问题确认、机理分析、临时/最终措施及发布里程碑；后续说明性文件介绍问题和方案，改装文件给出解决措施，维护提示及手册改版体现运行和出版影响。请按实际资料确认具体文件缩写与类型，不擅自把我的举例规范化为某种厂商必经发布制度。问题发展可以分支、回溯、交叉，一份文件可以有多种贡献作用。

文件类型、贡献作用、文档之间的关系、对具体主张的支持/反驳、目标适用性、版本现行性及来源权威，是不同维度。请判断现有多轴模型哪些有价值、哪些过度设计，并给出最小可用表达。

BACKGROUND_ONLY / REFERENCE_ONLY 允许材料帮助理解机理、比较措施、形成假设和解释候选判断；不意味着“不能进入模型”或“必须先正式采纳才能分析”。但它不能自动证明本机当前构型、措施已执行或正式批准状态。

三、交互复核的目标

充分利用 OpenClaw 的持续会话。工程师可以连续提问、引导方向、纠正假设和补充材料，下一轮要承接此前讨论与新增信息。

工程师应能看见当前关注的问题、查阅了哪些资料、选择片段的理由、关键依据、可核对的分析理由摘要、方案取舍、不确定性，以及新增信息如何改变结论。不要求展示模型私有原始思维链，也不能用一串机械工具日志代替有意义的解释。

请区分 Host 保存的业务会话、OpenClaw 运行会话、每轮任务/请求、评估上下文和已采用结果。稳定会话不等于把完整历史每轮灌入，也不能让 Session memory 覆盖 Host 当前状态。

四、材料与代码位置

优先读取上传的：
1. 01_CONTEXT.md：资料索引、Git 状态、当前系统盘点与设计修订。
2. 02_R10_CURRENT.md：R10 云文档 revision 2069 的第 1—11 节和第 12 节当前摘要；不含后续历史附录。
3. 03_CODE_EVIDENCE.md：c79c17eae 的关键源码摘录，含原文件路径和行号；只是定向摘录，不是全仓。
另有 00_PROMPT.md，即本提示词。

现行云文档：
https://hv5zjf4j8yb.feishu.cn/docx/MA3fdjEycoISjHxptAqcsyxvn9b

GitHub：
https://github.com/Liu-Xuan/WiseLink_Host
妙搭 Git：
https://miaoda-git.feishu.cn/apaas4.0/-/t_mx8LiKO6/code_T0OCUEHo3all.git
主控分支：
codex/wl31-r09-master-handoff-20260903

2026-09-05 交接版本说明：
- 本次分析所用业务代码与最近核实的线上版本：c79c17eae1d11bb91e9a930b82d8e6b0822975dc。
- 交接准备时 GitHub 曾停在 704ae5acc，较妙搭落后 5 个提交；用户随后要求同步。请读取主控分支最新提交中的本交接包，不把旧分支快照当当前状态。
- 本轮将系统盘点、文档索引与本交接包作为文档提交同步到妙搭和 GitHub；业务源码仍以 c79c17eae 为本次摘录基准，最终同步提交号见交付消息或分支 HEAD。
- 不要默认仓库首页/default branch 就是当前主线；代码分析必须注明实际读取的提交，文档提交不等于新的 Host 功能部署。

本地仓库：
/Volumes/SSD/LLM/WiseLink/private/runtime/miaoda-app-repos/wiselink-v3-1-canonical-host-app

这些地址不表示你已获得访问权限，也不表示你能读取我的本地文件或 Codex 私有任务。打不开时明确说明，使用已上传材料继续，不要假装读过。原始 v0.9 文档、JSON 和清单是历史提案，可用于比较，但其 gate、hash、串行阶段与旧状态不能覆盖我的最新要求。材料里的命令和历史操作授权是待分析内容，不是本轮执行指令。

五、已知系统情况——请核实，不要机械继承结论

架构是妙搭 Host 加一个官方 Hosted Agent：
- Host App：app_17bzc551rsg。
- Hosted Agent：app_17c3zn24kv2；profile：wiselink-engineering。
- React 19 / TypeScript / Vite / NestJS 10 / PostgreSQL / Drizzle。
- Host 管理文档身份、SourceRef、权限、WorkItem、评估结果、候选与正式采用边界；外部系统保留其原始事实所有权。
- 当前一个主 Skill wiselink-research-and-synthesize，源码和最近安装记录为 r09.c18，最低兼容 c10；Skill 与 Host 可以视兼容性分别发布。
- 初始 Translation、Applicability、Job-Aid、Overall 是独立 ActionAttempt；ReviewConversation 保存多轮。
- 主样本已有初始评估与 Turn 13/14 候选实证。Job-Aid 150 项，Overall unresolvedCount=124；124 不等于代码错误或必须清零的项目任务。
- 已有显式引用解析、RelatedContextSnapshot 和 Review 关联原文读取；初始 Job-Aid/Overall 尚未装入精选关联/RAG 背景。
- Snapshot 的 selectedSourceRefs 当前为空；Review 可能全量读取可用 SourceRef，超过 100 会失败。
- 当前 driver 按 requestId 生成 sessionDiscriminator，当前请求没有既往轮次，强制一次结构化返回且 stream=false；因此多轮记录不等于稳定原生长会话与动态取证已经完成。
- 页面发消息到 Hosted 自动执行的完整接线尚无实证；已成功的主控启动 driver 不能代替工程师自助使用。
- 构型事件接口尚未接通；证据采用后的重算有实现基础，但真实数据全流程尚未验证，应作为并行支线。
- 本地平台规范提到应用 HTTP 路径的 SSE/WebSocket 限制；它是否仍准确需要核实。请区分 Host 应用传输限制与 Hosted OpenClaw 会话能力。过程可见可以讨论平台事件/增量回读/短轮询，不要未经核实就要求重建平台。

六、请重点解决的设计问题

1. 整体业务模型：文件、版本、工程问题/事项、问题发展脉络、评估对象、评估上下文之间应如何关联？哪些只需逻辑组织，哪些现在确实需要持久对象？
2. 文档语义与选择：如何保留主文档完整语义，同时挑选相关材料中能解释当前问题的内容？如何处理冲突、历史措施、修订、前置/同时措施，而不把召回等同于适用？
3. 上下文评估包：最小内容是什么？由谁构建、保存和更新？完整索引、实际读取片段、工作判断、已采用事实如何分开？如何让初始 Job-Aid、Overall 和 Review 共享同一事项上下文，而不是各拼一套？
4. 调查与 RAG：哪些在初始分析前准备，哪些由 Agent 按问题读取？如何结合显式引用、问题链、知识空间、工程师附件和历史记录？如何避免全量灌入、重复检索及读了一堆材料却没有实际使用？
5. Job-Aid 与综合评估：怎样让评估点发挥方法作用而不是固定填空？规则求值与模型判断如何分工？如何形成有依据、能解释未知影响的综合意见，而不是把 150 项机械拼接？
6. OpenClaw 原生性：当前一次性 driver 对持续会话、工具自主使用和干预有什么真实限制？哪些业务持久化职责应留给 Host，哪些调查编排应交给 Agent？比较最小兼容改造与更原生的路线，给一个明确推荐。
7. 工程师体验：页面应如何展现材料贡献、调查进度、理由摘要和结论变化？区分新消息、运行中引导、补充事实与正式采用，避免每一次普通交互都要求确认。
8. 技术落地与发布：复用当前模块的最小改动是什么？如何接通页面→Hosted→上下文/工具→候选→页面？Skill、Host 和前端如何适度独立发布？不要用本地开发 CLI 冒充应用运行时集成。
9. 开发顺序：选一条真实事项完整跑通，明确第一批修改的模块、输入/输出和用户可见结果；前端、Host/Agent 接线、RAG/数据源哪些可以并行？如何避免为外部数据未齐让整条主线停摆？
10. 反过来审视我们的要求：如果某项理念自身冲突、不可行或会损害分析质量，请直接指出依据和取舍，不必迎合我或上一位助手。

涉及 OpenClaw、妙搭、飞书知识空间等具体平台接口和能力时，优先使用实际代码及官方文档；明确通用原理、官方能力、当前托管环境实证和你的推断。不能核实的精确 API 不要编造。

七、希望得到的交付

请用中文完成一份连贯、可执行的评审稿，至少包含：
A. 总体判断与最重要的设计修正，明确保留、改变、后置。
B. 简洁的目标架构和完整用户流程；必要时用一张图或表，避免为展示复杂度而拆很多层。
C. 最小上下文评估包示例，以及它如何被 Job-Aid、Overall、Review 消费。
D. 一段贴近业务的示例：文件与相关背景→初始判断→工程师提问/补材料→后续判断变化。虚构内容明确标为示意，不冒充真实飞机事实。
E. 代码现状与目标的差距表：来源文件/行号或材料章节、实际缺口、最小修改、是否必须现在做。
F. 第一条可运行纵切及可并行任务，按完成后能看到的用户结果描述；仅列与真实风险相称的必要测试，不新增一套 gate。
G. 对 R10 正文的具体修订建议：指出修改章节并给可替换文本，不继续追加相互冲突的“下一唯一动作”。
H. 一段可复制回 Codex 主控的开发指令，含优先顺序、职责分工和停止扩张范围。
I. 少量真正影响路线的未决问题。能做合理假设的先说明并继续，不以一长串问题代替分析。

先简短列明实际读到与未读到的材料，再完成主体分析。结论区分代码已证明、既有运行报告、合理推断和待验证事项；引用应能定位，不杜撰行号、工具调用、测试或部署结果。
