# R10 Satin 前端接入与真实运行记录

日期：2026-09-05（Asia/Shanghai）。这是生产集成与实际页面记录，不把原型离线检查算作 Hosted 验收。

## 已集成的前端

| 增量 | 前端源提交 | 主控集成提交 |
| --- | --- | --- |
| A：Silver / Carbon Satin 材质、布局、连续阅读 | c3b7733e1 | 453be9d4f |
| B：本次材料、真实 execution 与只读刷新 | 9d29e86fc | 51ef9361c |
| 按 Host 支持范围发送 AUTOMATIC | de6711dd3 | f66a643dc |
| 规则默认展示与提交焦点统一 | 70a74167c | 8fd75218d |
| 后台刷新可编辑草稿、旧投影提示 | 5d2e188c1 | d620b22c8 |

前端由既有任务「WiseLink 前端与 v0.6 接续」交付，主控负责 shared/Host/Skill、集成与发布。设计参考见 [UI-N02](design/r10-satin/README.md)，没有导入静态业务脚本或虚构材料。

## 已核实发布

| Release | Commit | 实际结果 |
| --- | --- | --- |
| 7681905703317687513 | 453be9d4f | finished；Satin A 与 Host 自动执行意图/回读 |
| 7681909446016257323 | e6256c03c | finished；自动领取 SQL 别名修复 |
| 7681916882333011151 | f66a643dc | finished；Satin B 与页面自动发送 |
| 7681921550199491764 | 2110b5c26 | finished；来源权威语义映射修复 |
| 7681924845857262806 | 4edb3a376 | finished；UUID 快照引用及默认规则焦点同步 |
| 7681935244031429931 | d620b22c8 | finished；后台刷新可编辑草稿、旧状态明确提示 |

以上发布均回读到对应 commit，error_logs 为空。最终后续发布与回合结果在本文件继续补充。

## 真实页面观察

对象：SB 777-34-0425，WorkItem `WI-2c1902db-c2cd-427d-b0d4-1f8f70fe6597`，原事项 revision 11；已登录用户操作。

- Silver 和 Carbon 两种实际页面均已查看；深色工作空间中 PDF 保留白色原件，没有反色。
- 本次材料按需展开：主文件、15 个已保存回合（首次检查时）、历史候选 SourceRef；10 处正文引用按 Host 结果合为 6 个目标。未把选入、实际读取、候选引用和正式采用混作同一状态。
- 点击真实历史候选的 `ad7153…c763764cbe17` 来源后，受控 PDF 定位到第 5 页的 SL 引用，证据栏回读 1 个内容单元和 1 条来源。此前复核页 0/0 是未加载/未选定位内容，不是文件不存在。
- 页面显示“发送并分析”，只在 Host 明确返回 `automaticExecutionAvailable: true` 的支持范围内使用；不支持时明确只保存。

## 云端原生自动执行

官方 Hosted `app_17c3zn24kv2` 已安装 Skill r09.c19；最低兼容版本仍为 r09.c10。原生 command cron `8db789fa-4fe9-4e58-850c-97b4cd70eb46` 每 60 秒执行一次已安装消费者，范围为上述已授权样本；不依赖 Codex 手工 driver、开发 CLI 或开发者电脑常驻进程。

空闲自然 tick 已回读 IDLE/exit 0，不消耗模型调用。以下非空回合全部由真实页面创建、原生 cron 自动领取，未手工启动其业务 driver。

| 回合 | 实际过程 | 原因与处理 |
| --- | --- | --- |
| Turn 15 | 13:27:14 保存；13:27:31 自动开始；13:27:32 取消 | 上下文中来源权威的重复 `authority` 别名触发模型保护。Host 2110b5c26 仅向模型保留已有 `sourceAuthority`，原始快照不变。另发现 UI 默认展示 GOV-008、提交却为 GOV-001，前端已独立修复。 |
| Turn 16 | 13:45:50 保存；13:46:29 自动开始；13:46:30 取消；实际焦点 GOV-008 | `snapshotRef` 内嵌 WorkItem ID 命中控制标识保护。离线扫描完整真实输入，唯一命中为 `context.relatedContext.snapshotRef`；改用已有普通 UUID 引用，事项/版本仍保存在快照正文中，不恢复内容 hash。 |
| Turn 17 | 14:00:34 保存；14:01:23 上下文准备失败；数据库实读默认焦点 GOV-008、AUTOMATIC=true | 原文包元数据返回 null；ActionAttempt 正确记录 FAILED / claimCount 0。其后页面也因相同原文读取失败无法刷新。官方存储接口返回空列表/0 文件；用户确认未操作，尚待平台侧查明存储或可见性异常。 |

Turn 15/16 两个失败均保留历史输入与错误状态，未调用模型、未写入候选。故障诊断只读原生运行记录及私有 checkpoint，并以离线依赖桩阻断所有远程调用；未重放业务动作。

Turn 17 同样未调用模型、未写入候选，未重新执行。完整时间、文件定位、Trace、执行主键和恢复范围见 [存储异常记录](WL31_HOSTED_STORAGE_INCIDENT_20260905.md)。13:57 前的页面/来源成功证据不表示 14:01 后存储仍健康。

前端 d620b22c8 修复正常后台只读刷新误锁输入器：可继续编辑文字和附件；提交/确认/关闭等写动作仍保持原有防重复和读写串行。刷新失败独立标记旧投影，只在成功返回新结果后清除；权限失效仍清空不可访问内容。它不修复存储，也不伪造最新执行状态。

## 验证与剩余工作

Satin B/自动接线联合 7 suites / 72 tests、前后端类型检查与生产构建通过；焦点修复 3 suites / 40 tests、client 类型与构建通过；Host 上下文修复定向 19 tests、server 构建通过。检查只覆盖相关改动，未增加 gate、hash、冻结契约或第二业务状态机。构建仍有既有父工作树 tsconfig 扫描、模块类型和 chunk-size 警告，未静默压制。

草稿编辑修复在主控集成后，相关 4 suites / 46 tests、client 类型检查及生产构建通过；由于原文存储异常，尚不能将其线上正常讨论场景记为已验证。

自动候选回写与后续追问需先恢复原文存储后继续验证；不能把自动领取或失败可见当作完整闭环已完成。评估前共同上下文、稳定跨轮模型会话、真实取证活动/steering、知识空间 RAG 与构型源仍是后续工作。历史 Turn 13/14 保留其真实 c16/c17 来源记录。

本轮未确认 ReviewAction、未正式采用、未关闭讨论，也未由候选执行修改事项 revision/current/STALE。
