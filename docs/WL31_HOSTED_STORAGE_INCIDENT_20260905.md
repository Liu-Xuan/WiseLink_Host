# Hosted 原文存储读取异常

记录日期：2026-09-05，时间均为 Asia/Shanghai。范围：Host `app_17bzc551rsg`，样本 `WI-2c1902db-c2cd-427d-b0d4-1f8f70fe6597`。这是实际运行异常记录，不是新的发布 gate 或数据契约。

## 已确认的事实

- 13:57:33，FileService 对原文包返回有效元数据：文件 ID `1875042263478407`，桶 `bucket_aadkprardjghu`，1290694 bytes，`application/json`。成功 Trace：`5fa0e6824ca52cb357084f6d5d635d5f`。
- 14:00:34，页面创建 Turn 17。无 criterion URL 参数；数据库实读评估点 `GOV-008`、自动执行意图 true、`CANDIDATE_UNADOPTED`。
- 14:01:23，原生 Hosted 消费者自动领取；原文包元数据返回 null，Host 在上下文装配阶段写入 FAILED。Trace：`d1df4cde8cf11f796e732c35059daedb`。
- 同一 Trace 内另一包的一次 `fetch failed` 已被现有单次传输重试处理，随后同样返回 null。不是重试未生效，不需要新增重试框架或放开完整性检查。
- 14:14:15 左右，刷新整个页面同样在读取原文包时 500，Trace：`f53ce38f7203bfa37cce086c4dbfcf6b`；因此旧页面的 REQUESTED 并非最新执行状态。新页面明确显示暂时无法打开。
- 14:20 左右，官方 `apps +file-get` 返回 `400000034: File not found or no access`；按精确文件名及整个应用查询的列表为空，用量接口返回 0 文件 / 0 bytes。近两小时 Host 文件日志未发现 `remove` 调用。这不足以断言物理删除，也未证明是某次发布导致。
- 本地项目运行目录和官方 Hosted 既有运行目录/缓存未发现与现有原文包完全匹配的备份。Hosted 仅持有上下文/来源片段和引用，不能据此重造原件。
- 用户随后明确回复“没有进行过操作”。这是本人未执行清理、恢复或配置变更的确认；仍需平台侧查明存储不可见的原因。
- 14:36，纯前端修复 release `7681935244031429931` / `d620b22c8` 已 finished，再次官方文件查询仍返回 `400000034`，日志定位 `2026090514362846B9313531C4886F8195`。技术发布没有恢复原文存储。
- 15:29，官方用户身份再次读取同一原文包仍返回 `400000034`，日志定位 `20260905152905D2B30D98B5385B7564FE`；应用文件列表无过滤查询仍为空，用量仍为 0 文件 / 0 bytes。
- 15:38，数据库登记的原始 PDF 也返回同样错误，日志定位 `202609051538079E8DB37B6BDB99771698`。该文件是 `777-34-0425.pdf`、86742 bytes、对象 ID `1875042252601353`，位于同一桶的 `/document-management/source/sha256/3a/3a99b7a42da961a6c76b641b99971e40df58f2b2ec11253babeb4cdc37501ed8.pdf`。不是只缺少一个解析产物。

缺失引用的现有文件路径为：

`unified-parsed-packages/sha256/46753f3aef474e65caec3f92dd5340644b102b28fe4ede833c1248ada8459a88.json`

此 SHA 是已有产物标识，不是本轮新增的 hash 或基线。

## 深入排查：已定位的原因层级

用户要求“深入分析原因再继续”后，暂停 c20 安装及 Host 新发布，进行了只读的 Trace、SDK 源码、数据库及已登录浏览器交叉核对。

### 存储层：原桶文件不可见，最终原因尚未查明

- 13:57 成功与 14:14 页面失败的真实浏览器 Trace 都记录运行提交 `4edb3a37643e0be79b17247a08ea790afb108086`；对应文件日志进程号均为 1092。13:57 至 14:01 之间没有新的发布记录。不能将故障简单归因于切换到一版新代码。
- 成功和失败请求使用同一路径；同一工程师用户在 13:57 成功、14:14 失败，原生服务调用也失败。因此不是仅 Hosted 服务身份缺权限；仍不能排除平台对文件的全局可见性/权限变化。
- 主包、已有双语/分析包和原始 PDF 均受影响；数据库仍能读取原来的引用和业务状态。故障发生在文件字节/元数据的读取链路，不是数据库中事项消失。
- 管理后台文件页也显示空存储。进一步在浏览器网络面板实读官方管理页发出的列表请求：`/play/api/v2/storage/admin/app/app_17bzc551rsg/bucket/bucket_aadkprardjghu/list`，请求体只有 `{ "limit": 200 }`，HTTP 200，正文为 `{ "data": { "attachments": [], "hasMore": false }, "status_code": "0" }`。当前管理端仍查询原来的桶，没有名称、路径或时间筛选；“管理端误指向另一个空桶”已基本排除。没有复制认证请求头或执行自造存储请求。
- 官方 FileService SDK 的 `getFileMetadata` 将 `k_img_ec_000034` 归一为 null，也会在响应无 metadata 时返回 null；Host 随后报 `ARTIFACT_READBACK_MISMATCH:METADATA`。所以 null 不足以区分索引/可见性异常和真实文件删除，更不是 hash 或字节校验误杀。
- 13:45—14:20 的 Host `file` 模块 `remove` 查询为空；代码内相关删除入口只处理单个 attempt-owned candidate，不是清空主包。管理台本轮提供的是登录审计，没有文件级操作/恢复记录。没有足够证据确认物理删除或认定操作人。

可行动结论：需由妙搭存储侧依据原桶、两个文件 ID 和成功/失败 Trace，检查对象索引、可见性/授权、生命周期或清理记录及可恢复性。普通重新发布、修改 Skill、放宽校验、给空结果加重试都不能恢复这些字节。

### 应用层：原文失败被放大为整页不可用

- `CanonicalHostVerticalService.page` 在返回工作台数据之前先执行 `reader.inspectSourcePackage`。原文读取抛错后，整个 `document-parsing` 请求失败。
- `DocumentParsingPage` 初次加载失败就清空 pageData 并提前返回 LockedState；`ContinuousReviewPanel` 位于后续正常渲染分支，因此无法挂载和调用它原本独立的对话回读。
- 已有 `ReviewConversationService.current` 的职责是对象授权、读取已保存讨论和投影 ActionAttempt，不要求读取原始文件；但当前页面把这个能力绑在了完整原文页面成功之后。
- 冷刷新实测显示“暂时无法打开当前工程事项”，并提示检查链接/权限。当前故障实际是文件不可读，不能把所有服务错误都表达为用户链接或权限问题。
- 直接在地址栏打开 Review GET 被平台 CSRF 检查拒绝；这不是该业务接口不可用的证明，也没有尝试绕过 CSRF。后续应通过既有客户端 SDK 的正常认证请求验证独立回读。

这是应由项目修复的加载依赖和错误呈现问题：原文不可读时，应仍能在原有身份/对象授权下读取讨论与真实终态；来源阅读单独报错，不伪造 FRESH_READ、材料已读或候选结果。

### 工作流层：恢复存储不等于 R10 已完成

Turn 15/16 的模型前输入兼容错误是本项目的投影问题，已各自修复；它们与 Turn 17 的文件不可见是不同原因。自动领取已有真实证据，但三个自动回合都没有生成候选，不能把它们记为自动分析闭环通过。

当前 driver 按 requestId 派生调用标识，先按程序选定来源并读取，再强制生成一次候选结构；尚无稳定跨轮会话、智能体按问题继续调查和真实活动展示的完整运行证明。结论依据实际 driver 编排，不依据 Chat Completions 或 stream=false 这两个参数推断 OpenClaw 本身的能力。

`90b05aa8f` / c20 的共同背景任务输入增量已在深入定位后推送、安装并随 `d5ffbf4c9` 发布；它不包含上述持续调查闭环。现有测试和安装/发布成功都不能替代真实页面连续往返。

## 新文档对照测试（用户明确要求，16:15—16:36）

同一 Host `app_17bzc551rsg`、同一原桶 `bucket_aadkprardjghu`，通过官方文件接口上传独立测试文档，不建立 WorkItem，也不触发解析或评估。

| 检查 | 实际结果 |
| --- | --- |
| 新文件 | `WiseLink-storage-test-20260905-1614.txt`，498 bytes，`/1875478528545995.txt`；上传时间 16:15:37 |
| 立即读取 | 元数据及下载成功；下载与本地内容 `cmp` 一致 |
| 16:21 延时读取 | 元数据及第二次下载成功，`cmp` 再次一致；距上传约 5 分钟 |
| 16:36 本次发布后 | 同一路径元数据与第三次下载成功，`cmp` 仍一致；本次 `d5ffbf4c9` 发布未使测试文件消失 |
| 无过滤列表/用量 | 列表仅新文件、`has_more=false`；1 文件 / 498 bytes |
| 同时读旧 PDF | 仍为 `400000034`，日志定位 `20260905161609ACB44DE5F28820635F77` |

结论收敛：**当前并非全桶无法读写，新对象的保存和短时持久读取正常；旧对象仍不可见或不可访问。** 尚不能区分旧对象删除、索引异常或对象级授权变化。此测试验证的是官方文件存储链路，不覆盖完整上传、解析、来源登记和评估流程，也不证明更长期持久性。

测试文件明确标注非工程依据，暂留在原桶供后续对照，不替代任何旧文件、不更改既有引用或业务状态。没有新增内容 hash、基线或 gate。

## Turn 17 的真实状态

| 字段 | 值 |
| --- | --- |
| ReviewConversation | RC-c92b2e97-3b6c-4d78-a59d-8c90e4046cf5 |
| Turn | RT-31d4f979-5b24-49b3-9e67-a62575008b21 |
| requestId | 9029d6b6-dd8b-4486-b95a-de695832b208 |
| ActionAttempt | ATT-38218e57-8186-49df-b1d6-54a24ef869e6 |
| operationRef | AQ-5692dc36e8934104aaad6b3d0b8a3d62 |
| status / claimCount | FAILED / 0 |
| errorCode | ARTIFACT_READBACK_MISMATCH:METADATA |
| terminalReason | REVIEW_CONTEXT_PREPARATION_FAILED |
| 任务输入 / 模型 / 候选 | 均未生成 |

事项 revision 11、讨论 ACTIVE、历史候选仍保存在数据库。没有 ReviewAction 确认、正式采用、重新解析或业务版本修改。原生消费者已自然返回 IDLE，未重放失败 Turn。

## 后续处置

1. 页面依赖已修正：`100709797` 随 `d5ffbf4c9` 发布后，实际登录页冷刷新已显示事项 revision 11、Turn 17 FAILED、错误 `ARTIFACT_READBACK_MISMATCH:METADATA`、操作记录 `AQ-5692dc36e8934104aaad6b3d0b8a3d62` 及历史候选；来源定位禁用，没有新回合或采用入口。第一次过早 AX 观察仍是旧画面，刷新完成后实证成功，未据此另造补丁。历史可读不等于旧原文或新分析恢复。
2. 同时交由平台侧核查上述原桶、文件 ID、路径与成功/失败 Trace，区分存储不可用、权限/可见性变化与实际删除；如涉及清理或恢复，查对应操作记录。当前本地与已读 Hosted 缓存未发现完整匹配原文包，不从片段重造或改写数据库引用。
3. 存储恢复后，从实际页面新回合验证已发布的 c22 跨轮授权会话和按问题取证，完成自动候选与追问。恢复、候选回写、连续使用分别据实记录；不重放失败回合、不重跑完成的 InitialAnalysis。

前端的刷新/草稿交互修复可独立交付：旧投影须明确标识，只读刷新不阻止编辑草稿，真实写入仍防重复。它不能修复云端存储，也不作为存储恢复证明。

## c22 本轮存储复查与 22:23 真实页面请求

- 对原桶既有测试文档 `/1875478528545995.txt` 再次执行官方元数据读取与下载，完整下载 498 bytes。本次未再次与最初本地文件逐字节比较；不把此前三次 `cmp` 写成本次证据。没有另上传测试文件、建立 WorkItem 或触发评估。
- 官方文件列表按 `777-34-0425.pdf` 查询仍为 `items=[] / has_more=false`。这是名称列表结果，不是新一次精确文件 GET 的错误码证据。
- 22:23:09，SB 事项 `WI-2c1902db-c2cd-427d-b0d4-1f8f70fe6597` 的真实 document-parsing 请求在 `readActualBytes` 元数据阶段失败，Trace `db969316260ecbc9e6609e1260b04a1a`。
- 22:23:12，旧事项 `WI-4cc3fcaf-7945-499d-8ef5-917a180e3f7b` 同样失败，Trace `34875778beab424a98ea66707b692172`。
- 22:23:16，当前页面所选事项 `WI-e3e3bfe4-a800-4244-8269-498d39f4ca4f` 同样失败，Trace `197b08501fb5f9b89c73c9ce4001ab39`；原生界面显示资料库读取错误。三条 app_server 错误均为 `ARTIFACT_READBACK_MISMATCH:METADATA`，未进入智能体调用。前端日志仍标记旧 `73561af4d` bundle，不能将该观察表述为 c22 新 bundle 完整重载验收。

结论：至少三个旧事项受原文元数据读取问题影响，不仅是首个 SB。新对象通路仍可用，但没有证明旧对象的物理删除、操作人或可恢复性；仍需平台文件级记录。c22 发布成功不修复存储，也不改变以上证据边界。只记录错误码、路径和 Trace 摘要，不将原始请求头或日志整包纳入交接资料。

## 23:31—23:45：旧清理机制、定位方式与完整登记范围

- 对线上数据库的 11 个 WorkItem 去重，得到 7 份原始 PDF；另有 2 份尚未挂到 WorkItem 的历史 SourceArtifact。9 份源文件均按数据库原始路径执行官方 `file-get`，全部返回 `400000034 / File not found or no access`。SB 主解析包同次精确 GET 也失败。不是仅根据资料库的 5 条最近访问或一个 METADATA 文本推断全量失联。
- 7 份已挂事项 PDF 分别为 `777-FTD-31-21002`、`737-34-3830`、`787-FTD-46-14004`、`777-34-0425`、`777-SL-31-064`、两个明确标作 Related_Context_Positive_UAT 的 `777-FTD-31-21014/21015`。部分文档有重复开发运行，7 个物理源文件不是 7 个 WorkItem。
- 23:31，新测试文件 `/1875478528545995.txt` 元数据仍可读，498 bytes；本次没有下载或重新比较字节。无过滤文件列表为 2 个对象、166301 bytes：该测试文件和 19:15 上传的 c21 ZIP。旧对象仍不在列表内。
- 官方 `automation-list --all` 为空；最近 7 天 Host 的 `file` 模块按 `remove` 检索为空。此证据只覆盖应用触发器和可查应用日志，不覆盖平台后台生命周期任务或全部管理员存储操作。
- 用户提到的旧清理机制有对应记录：旧 0.11 PT60 的 `Development_Test_Data_Cleanup_Policy_Readback_Result.md` 明确 `policy_readback_only`，只定义诊断/回放/沙箱产物的清理语义，没有执行 worker，也没有接入当前 Host 文件桶。当前构建清理只处理本地 dist/依赖；云端 remove 仍仅处理 attempt-owned candidate 的限定目录。
- 上传 SDK 已完整执行 `preUpload → PUT → object/callback → metadata`；其公开 UploadOptions 没有临时/永久/TTL 参数。下载每次重新生成签名，而本次失败在签名前的元数据读取，不能解释为旧签名过期。
- 前导斜杠归一化及上传路径修正于 8 月 14 日已完成；同日已用稳定对象 ID 替代 updatedAt 作为 providerVersionId，时间只作审计。相关适配器、目录常量和依赖从事故运行的 `4edb3a376` 到本轮修复前 HEAD 没有差异。它们不是 13:57 成功、14:01 失败之间的新变更。

当前最窄结论仍是：**旧对象在原桶的元数据/可见性链路失联；项目侧全桶清理、链接到期和事故时路径迁移均没有支持证据。平台清理、索引或对象权限变化仍待文件级记录确认。** 尚未取得平台文件级原因或恢复结果，也没有将提交工单写成已完成。

## 本轮实际修复与清理准备

1. 后端 `readActualBytes` 保留原有通过条件，将 METADATA 错误细分为 `NOT_FOUND_OR_INACCESSIBLE / BUCKET / PATH / LENGTH / MEDIA_TYPE`。只改善定位，不新增校验、重试、内容 hash 或 gate，不等于恢复文件。
2. 资料库回归源于 Satin 提交 `453be9d4f` 的状态互斥隐藏：无 projection 藏预览与快览，有 projection 藏列表。原文读取失败后全部退回最近访问导航。前端源提交 `a1f501c99` 恢复稳定列表与 324px 快览，保留单对象受控读取、来源错误及完整目录未接通的真实边界；主控补充直接 Error 携带 NOT_FOUND 后缀时的分类兼容。原型 fixtures、责任筛选和机型数据没有搬入生产。
3. 私有备份仅保存在本机任务临时目录：11 个 WorkItem、2 个 ReviewConversation、18 个 Turn、18 个工程师输入、1 条配置查询以及 69 条源文件/文档登记均通过官方导出备份。73 条 ActionAttempt 的整体 SQL 导出超过平台 1 MB 上限，已按精确 WorkItem 分成 11 份 JSON，只读回查 73 行全部落盘。没有把这些私有数据放入仓库或 GitHub。备份不包含失联原件，不宣称做过完整数据库恢复演练。
4. 清理会涉及历史讨论和评估记录，不仅是最近访问列表；目前没有执行文件删除或数据库 DELETE。两条 8 月遗留 RUNNING、1 条过期 QUEUED 和 2 条 WAITING_INPUT 没有有效租约，状态不能当作活跃执行证据，也不能默默改成成功。清理前应统一处理这些记录与仍指向旧事项的原生消费范围，不能只删 WorkItem 留下关联和持续错误。

## 防止正式运行复发：接下来要落地的存储工作

以下为本轮用户纠偏前的建议，**实施优先级由文末“根因优先续查”和当前执行计划替代**。恢复副本、恢复演练及同步保护等级不再是当前开发主线；保留这些旧建议不代表仍获准实施。

- 用实际对象定位承接持久数据。源 PDF 已登记 bucket/path/object ID；普通解析产物目前只有逻辑 ref 并重查默认桶，应由 Host 在现有持久描述中保存实际 locator，读取使用原 locator。locator 不能由模型或浏览器任意指定；旧描述保留明确兼容。此改动防默认桶漂移，不是本次事故已证根因，当前尚未实现。
- 原件与必要解析产物须有明确保留和恢复能力，数据库引用备份不能替代字节备份。先确认妙搭对象保留、文件级恢复和清理审计语义，再接入一个受权限控制的恢复副本；不把临时目录、签名 URL 或 Agent 记忆当生产备份。不为此新增第二套业务真值、hash 或通用 gate。
- 真实验证使用产品正常受理入口，并覆盖上传后读取、发布/重启后读取、经过实际保留周期的再次读取及一次恢复。当前 498-byte 官方 API 探针只证明对照链路可用，不能替代此验证。
- 授权目录 GET 与快览应独立于原 PDF/解析包读取；当前修复只恢复稳定框架，完整的授权检索、分页和数据库轻量快览仍待接通。

## 2026-09-06 续查：数据位置、数据库日志与恢复预览

本节记录截至 01:13 的续查。用户要求继续分析旧文件清理、元数据/链接变化及数据库日志；本轮没有删除云文件或业务记录，没有应用时间点恢复，也没有修改审计开关。

### 同一 Host 应用的两套持久服务

| 内容 | 保存位置与现状 |
| --- | --- |
| WorkItem、文档登记、版本关系、讨论、工程师输入和 ActionAttempt | Host 应用的妙搭 PostgreSQL；相关记录与精确主键仍可查询 |
| 原始 PDF、统一解析包等文件产物 | Host 的 FileService 对象存储；原桶 `bucket_aadkprardjghu` 中的旧对象不可读，同桶新测试对象元数据仍可读 |
| 路径、对象 ID、字节长度及已有内容标识 | 数据库或持久 Artifact 描述中的文件引用；引用仍在不代表文件字节可读 |

正式数据并非只存在开发者电脑。本机导出只是本次事故取证副本，不含失联文件字节；Git 提交、技术发布和智能体缓存均不等于数据库或原件备份。

### 有 SQL 执行日志，缺少行变更审计

- 线上 `+db-audit-status` 返回 40 张表全部 `enabled=false`。随后读取同一官方 `audit_status` 接口，原始 40 条记录也明确返回布尔 `false`，排除了 CLI 对缺失字段默认填 false 的可能。
- 查询 `work_item`、`action_attempt`、`dm_source_artifact`、`dm_document_version`、`dm_acquisition`、`review_turn` 在 9 月 5 日 13:40—14:20 的行审计，六表全部因未开启而被跳过。空结果不能解释为未发生增删改。后续开启审计也不能补回此前的行前后值和操作人。
- 同时间段 `+db-changelog-list` 无记录。该能力查询表结构变更（DDL），不等于业务行历史。
- 13:57 成功 Trace 的 4 条数据库日志均为 SELECT；14:01 失败 Trace 的 10 条数据库日志中，9 条可解码：5 条 SELECT、1 条 INSERT 和 3 条 UPDATE，写入均涉及 ActionAttempt。另 1 条诊断 SQL 的正文被平台截断，不能完整分类。
- 扩展到 13:57:00—14:02:00，完整分页共 97 条 `data` 日志：83 条 SELECT、6 条 UPDATE、2 条 INSERT、5 条未填写 operation 的队列 CTE 读取，以及上述 1 条截断诊断日志。可识别写入为三次 `identity_session` 最近访问时间更新、一次 ReviewTurn 新增、一次 ActionAttempt 新增和三次 ActionAttempt 更新；未发现源文件登记表变更或 DELETE/DROP/TRUNCATE 的证据。该范围不包含平台内部文件索引、对象权限和清理任务的全部记录。
- 非 RETURNING 写语句的日志 `result_count=0` 不用作“影响零行”的证明。按精确主键重新 SELECT：Turn 17 创建于 14:00:34.966，仍为 `CANDIDATE_UNADOPTED`；对应 ActionAttempt 业务创建于 14:01:23.076、平台 `_created_at` 为 14:01:23.217，状态为 `FAILED`。

摘要只保留时间、日志 ID、语句类型、表名、错误/截断标记等诊断字段，存于既有私有取证目录；没有把 SQL 参数、讨论正文、身份凭据或完整请求日志提交到仓库。

### 恢复预览与已存在的新记录不一致

对 `online` 环境预览恢复到 `2026-09-05T13:57:00+08:00`（实际请求目标 `2026-09-05T05:57:00Z`），CLI 输出零张表变化。已安装的官方 CLI 1.0.87 会把缺失的 `changes` 归一为空数组，把缺失/零值的预计耗时填为 30 秒，因此进一步检查了原始响应：

- 只读预览提交使用 `dry_run=true`，返回 `preview_request_id=1875477401550948`。
- 原始状态为 `preview_status=Success`、`tables_affected="0"`、`changes=[]`，没有 `estimated_seconds`。本次零变化来自服务端，30 秒仅是 CLI 默认值；没有据此认定 CLI 丢失本次变更。
- 当前精确读回的 Turn 17 和 ActionAttempt 均创建于目标时间之后，与零变化预览不一致。尚未取得平台对比较范围、可恢复数据点及此差异的解释，不能承诺 PITR 可用，也不能把预览当作数据库从未修改的证明。
- 没有调用 `+db-recovery-apply`。即使数据库恢复可用，也没有证据表明它同时恢复 FileService 文件字节。

实现依据：[官方 v1.0.87 恢复源码](https://github.com/larksuite/cli/blob/v1.0.87/shortcuts/apps/apps_db_recovery.go)、[审计状态源码](https://github.com/larksuite/cli/blob/v1.0.87/shortcuts/apps/apps_db_audit_status.go)。仅检查安装版本对应实现，没有升级或修改 CLI。

### 结论与平台所需证据

**已确认旧对象在文件存储中不可见，以及行审计、原件恢复验证两个可靠性缺口；对象失联的最终原因尚未确认。** 已查 SQL 不支持“源文件引用在故障窗口被清理或改链”的解释；旧 PT60 无执行 worker、签名前即失败、同代码/进程/路径由成功转为失败等证据，也未支持项目清理、签名过期和事故时发布迁移的解释。平台后台清理、索引或对象级授权变化仍需文件级记录。

以下平台核查材料已备齐，尚未发送工单或消息：

1. 对原桶的 PDF `1875042252601353` 与主包 `1875042263478407`，结合 13:57 成功和 14:01 失败 Trace，查询 13:40—14:20 的删除/回收、生命周期、索引及授权变化，明确对象是否存在、恢复方式和保留期限。
2. 对恢复预览 `1875477401550948`，解释 `online` 库在目标 13:57 之后已有新增记录，预览为何仍为零变化；需要比较范围和恢复点证据，不直接覆盖当前库。
3. 明确原件与解析产物的持久保留、文件操作审计及受控恢复副本能力。项目据此补齐实际 locator、关键业务表行审计和真实恢复验证；开启数据库审计本身不能防止文件丢失。

资料库稳定列表与错误分类已随 release `7682080634169658567` / `d1bd56b1d37ea06c4b0a88a8eb76853ecff451be` 于 9 月 6 日 00:01:52 完成，官方状态 `FINISHED`、`error_logs` 为空。本轮核对发布状态，没有重复发布；不将其当作新版页面完整验收或旧文件恢复。00:45 主解析包的官方精确 GET 仍为 `400000034`，日志定位 `2026090600450531FA48053149EA9F5B0F`。授权目录与轻量快览独立读取、实际 locator 和恢复副本仍待实现。

## 2026-09-06 根因优先续查

后续顺序修订：2026-09-06 10:25 用户明确要求继续业务开发，旧事故改为独立平台支持事项，不再阻断开发、联调或正常技术发布。本节保留为早间事故证据与当时授权记录；后续正常读取改造及获准新资料测试见 [当前执行计划](WISELINK_R10_EXECUTION_PLAN.md)，不据此关闭或解释旧事故。

用户明确将工作重心改回“原本可读的文件为何变得不可读”。本轮只查既有对象和事故 Trace，未上传或恢复任何文件、未改历史引用、未创建业务事项、未调用模型、未改审计配置、未提交工单。已有备份与事故证据保留；本节及当前执行计划替代上文尚未执行的恢复建设建议。

### 原始 Trace 补证：成功不止元数据，失败仍止于元数据

重新调用官方 `+trace-get` 和按精确 Trace 的 `+log-list --module file`，两条 Trace 的 `is_break=false`，文件日志均 `has_more=false`。这不代表包含文件服务内部所有操作记录。

| 原始记录（北京时间） | 本轮实际返回 | 证据边界 |
| --- | --- | --- |
| 13:57:33.682；`LOG7681926352564178179` | 主包 `1875042263478407` 元数据、原桶与原路径，1,290,694 bytes | 元数据成功本身不证明正文已读完 |
| 13:57:33.885；`LOG7681926352564194563` | 同一主包 `download`，`status=succeed` | Nest SDK 在 `downloadInner` 返回后、`content.blob()` 前记录成功；仅此日志不能证明正文消费结束 |
| 13:57:39.097；app_server span `ee287666f19b2b9e` | 同一页面请求最终 HTTP 200；运行提交 `4edb3a376` | 结合该提交 `page → inspectSourcePackage → readActualBytes` 的无降级正常路径：该次主包读取必须完成正文、长度和已有摘要校验后才能返回。由 Trace 与对应代码共同支持当时完整读取成功，不是本轮重新下载旧文件 |
| 14:01:23.405 / .691；`LOG7681927325236038585` / `LOG7681927325236104121` | 同一路径两次元数据响应 `null`；SDK 日志标记 `status=succeed` | 这里的 succeed 仅指方法正常返回 null，不是对象可读；未进入主包下载 |

成功 Trace 另有两个包 `1875053341497475`（创建于 8 月 31 日 23:30）和 `1875099329078343`（创建于 9 月 1 日 11:41）的元数据及下载成功记录；14:01 Trace 中对应路径也返回 null，其中一条传输错误后重试才返回 null。不同创建时间的对象在同一次失败请求中均不可读，值得平台按共同处理范围核查，但不能据此认定它们同时物理删除或推导具体 TTL。

13:57 Trace 的文件 span 为工程师身份；14:01 为服务调用身份。既有 14:14 同工程师失败仍是必要对照，不用这两条不同身份的 Trace 单独排除授权变化。Trace 的 file 子 span 只显示 Host SDK 包装层，未返回底层对象存在性、存储索引变更、删除主体或 ACL 判定依据。`is_break=false` 不能填补该证据缺口。

### 08:44 精确现状核查

- 主解析包原路径 GET：`400000034 / File not found or no access`，日志定位 `2026090608443236ED2A38F0E319B8B5CF`。
- 原 PDF 原路径 GET：同错误，日志定位 `202609060844322BC4FC19A03414B6B38A`。
- 已有官方测试文件 `/1875478528545995.txt`：元数据仍返回 498 bytes；本轮未下载或比较其正文，未新建其他对照对象。

### 可查能力与接入差异

- 已安装 CLI 的 file 域提供 get/list/sign/download/upload/delete/quota，不提供按对象 ID 查询删除/索引/生命周期/授权判定历史的命令。未据此断言平台内部没有日志。
- 依官方索引检查了 aPaaS 审计与安全合规模块。aPaaS 的 [审计查询](https://open.feishu.cn/document/apaas-v1/application-audit_log/audit_log_list.md) 使用应用 namespace，公开 `app_type` 为 aPaaS/Aily；[事件表](https://open.feishu.cn/document/apaas-v1/application-audit_log/audit-event-list.md) 没有明确列出妙搭 FileService 对象生命周期。未将本应用 `app_id` 猜作 namespace，也未把云文档删除审计当成妙搭文件审计，未申请额外租户审计权限。
- [官方 CLI v1.0.87 上传实现](https://github.com/larksuite/cli/blob/v1.0.87/shortcuts/apps/apps_file_upload.go) 是 OpenAPI 预上传、PUT、callback，路径由平台生成。当前 FileService 0.1.2 SDK 是 inner 预上传、PUT、callback，接收明确 bucket、自定义 `filePath` 和 `upsert:false`。两者均有上传完成回调，但入口、身份承载及路径选择不同，不能把两条通路视为完全等价。
- SDK 的公开 UploadOptions 没有 TTL/临时或持久参数，也未看到额外绑定调用；这只描述客户端实现，不证明服务端不存在引用/归属/生命周期规则。当前没有实际 A/B 上传实验或平台生命周期结论。

### 待平台逐对象回答的请求（草稿，未发送）

范围：`app_17bzc551rsg`、`bucket_aadkprardjghu`；对象 `1875042263478407` 与 `1875042252601353`，原路径见上文。成功 Trace `5fa0e6824ca52cb357084f6d5d635d5f`，首次失败 Trace `d1df4cde8cf11f796e732c35059daedb`，同工程师失败 Trace `f53ce38f7203bfa37cce086c4dbfcf6b`。重点窗口 2026-09-05 13:40—14:20 +08:00，变化重点在 13:57—14:01。

1. 请立即保留这两个对象及上述时间窗的元数据、映射、鉴权与操作记录；本请求不授权删除、覆盖或恢复业务数据。
2. 逐对象确认底层内容是否仍存在，以及元数据记录当前状态（包括回收/软删状态）；明确应用、桶、路径与底层 key 的实际映射。
3. 解释成功请求与失败请求的具体访问判定：缺对象、缺索引、归属不匹配还是授权拒绝；给出判断依据，不只返回通用“not found or no access”。
4. 给出窗口内删除/清理、归属变更、索引变更或生命周期执行记录：时间、触发主体/任务、规则与版本、范围、结果及相关请求 ID。无记录时说明查询范围与保留期限，不将“未查到”直接写成“未发生”。
5. 确认 inner SDK 自定义路径在 preUpload → PUT → callback 成功后是否已满足持久登记要求；是否还有引用绑定或后台生命周期条件，并说明它们与官方 OpenAPI 上传的差异。

**结论仍未闭合：本轮加固了最后成功读取与首次元数据失败的证据，但没有取得能区分删除、索引异常或归属权限变化的对象级记录。** 已准备上述请求，尚缺指定的平台支持入口或联系人。下一步应取得该层证据；如平台记录不足，再在授权隔离范围设计控制变量实验，不把短时重传可读作为根因或修复验收。
