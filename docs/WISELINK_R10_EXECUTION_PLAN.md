# WiseLink R10 当前执行计划

更新日期：2026-09-08。依据：[R10 云文档](https://hv5zjf4j8yb.feishu.cn/docx/MA3fdjEycoISjHxptAqcsyxvn9b) 与 [当前正文镜像](WISELINK_R10_CURRENT.md)。当前运行证据以本页 9 月 8 日记录为准；云文档及正文镜像 revision 2215 的运行状态保留原时点，按用户要求不随每次排障更新。9 月 6 日发布复核与数据库/存储续查见 [存储异常记录](WL31_HOSTED_STORAGE_INCIDENT_20260905.md)，此前功能发布和页面证据见 [交接与响应记录](WL31_R10_CONTEXT_HANDOFF_PERFORMANCE.md)，视觉交付见 [Satin 运行记录](WL31_R10_SATIN_HOSTED_ROLLOUT_20260905.md)。

主控交接：用户于 2026-09-07 指定 [WiseLink R10 项目主控（2026-09-07 接管）](codex://threads/01a079d1-918d-7af1-a283-75968ec294ea) 接替 [WiseLink R09 项目主控（接替旧主控）](codex://threads/01a06562-e90c-7340-9fd5-9a96cdd3073f)。新主控承接当前 R10 目标、已有业务授权和分工，并核对 [2026-09-06 接管任务](codex://threads/01a0726d-5e6e-7b70-b1b9-a7e84fd1d31b) 的存储调查结论。此次接管时本地 HEAD 为 `408de9f17`；工作区已有 AGENTS/技能整理及临时产物，独立保留。历史待办不覆盖当前执行顺序，最新推送边界见下一段。

Git 当前边界：**2026-09-06 用户要求仅向妙搭 Host 的 `origin` 推送，停止一切主动 GitHub 推送；2026-09-07 接管再次重申避免越权。** 该要求取代 9 月 5 日的 GitHub `codex/*` 长期授权；旧凭据、refspec、成功记录和下文历史发布说明都不构成继续推送授权。每次明确指定 `origin` 和单一源/目标引用，非强制推送；不删除 GitHub 引用、不改写公开历史。现有 `core.hooksPath=.githooks` 下的 `pre-push` 同时校验 remote 名称及实际 URL，仅接受本项目已核实的飞书目标。见根 `AGENTS.md` 与 [边界纠正记录](WL31_GITHUB_SYNC_BOUNDARY_20260905.md)。

## 2026-09-08 Goal 实施与真实运行进展

用户于 09:17 明确设定 Goal 并要求继续推进。当前实施目标为真实工程事项评估小循环：材料进入 → 保存当前认识 → 核对全部真实依据 → 连续纠正/补材料 → 保存待复看问题，允许本轮评估完成而尚无实施决定。同一真实 SB 在 M3/DLI 上的完整初始分析及各至少两轮连续 Review 继续作为实际验收要求。下面区分代码、技术发布和真实运行。

- **W1 已接到实际消费者：**Overall 新任务生成同一份 v2 阅读内容，列表/简报/详情共享结果身份；前提保留原文、工程师陈述等载体，关联文档可以独立支持判断，否定/条件/冲突完整保留，不强制实施字段或固定动作数量。旧 v1 任务可读。
- **W2 已集成：**事项工作修订、真实输入覆盖、pending、局部 claim delta、同请求/attempt/turn 精确重放与版本 CAS；分支交付 `502070a2e` 及成员锁修订 `02a189d65` 已同步最终文件树。0023 增加工作记录和不可更改的 Review 业务范围；真实隔离 PostgreSQL 测试验证双方成员的 owner/版本更新在事务提交前被锁阻止。官方 dev→online migration 已应用 39 项预览差异；线上已读回工作表 RLS、SELECT/INSERT 策略、成员锁所需原有权限、Review 范围列与不可变触发器，未扩大权限。
- **W3 已发布：**前端交付 `60ee5c42c` 已同步，事项目录位于 `/library?mode=matter`，实际事项路由 `/matters/:matterId`；创建、关联、同版简报、所有前提、原文返回、草稿隔离与回执都使用真实 API。正常页面已创建下述真实事项，单份主要材料及尚未覆盖状态已显示；有保存结果后的多层阅读、依据和连续更新仍待实际验收。
- **W4 已接线：**c4 任务由 Host 冻结事项范围/版本及完整依据身份，只向模型投影脱敏工作内容。文档引用、变化 claim 和 coverage 必须本轮实读；候选答复与工作修订同一事务保存。解释不更新工作版本，冲突保存 BASIS_CHANGED 回执，不覆盖新结果。正式采用、审批、实施决定继续分离。
- **c34 修复与兼容：**在全文两次批量纠正均收敛且仅剩至多 8 个单元时，允许原会话逐单元纠正，每单元至多两次且必须持续减少错误；最终完整保真校验不变。Skill 提交 `230f11639` 已推送 origin 并完成私有同名安装，28/28 文件摘要一致，installed 162/162 测试通过。Host/前端集成 `d448e6ed9` 随 release `7682997419248356280` finished。页面实测发现快览 SQL 的 JSON 成员运算符在托管查询链路触发 42601；等价改为 `jsonb_exists` 后，`89334ab48` 随 release `7683002047251696854` finished，真实页面已正常读回。仍未把技术发布写成完整业务成功。

本批本地验证：前后端类型检查及生产构建通过；Host/Review、Overall、权限和前端相关定向测试已通过，最终事项范围、实际读取闭包、事务接线和迁移等 6 suites / 61 tests 通过。保留已有父工作树 tsconfig 扫描及 chunk-size 警告。测试及数据库同步不能替代下述真实页面与业务验收。

真实运行已恢复核对：正常页面于 09:43 创建 M3 `WI-ccf6bff7-232a-4885-b973-9b79273f9645`，复用同一 `document_version_b91b79a4b966a02d8bab1cc0`，解析成功。Host scope 与唯一 cron 已按原授权迁到该任务；release `7682974366691396538` finished，来源仍为 `db1c65125`。自然调度翻译 attempt `ATT-bdbbaa29-1433-4001-9074-64aa1d5fb5bd` 于 09:50:48 开始，09:56:25 因 TRANSLATION_RULE_PREFLIGHT_REJECTED 结束，无完整候选。七轮 HTTP 均 200/tool_calls，约 337.5 秒，没有再次发生 300 秒 HTTP 中断；第 4 窗口两次纠正后剩 7 个单元、15 项错误，表现为相邻单元数字/标识串移。c34 针对此具体尾段失败修复，旧失败保留。消费者诊断后暂停且无在途请求，旧退避/错误历史保留。

11:13 正常页面以 M3 和同一已上传 SB 新建 `WI-a3ff7197-ad64-4678-9e7f-f596d6ee62c7`，同一 DocumentVersion、解析成功。scope 与唯一 cron 迁移后，11:22:43 自然 tick 运行翻译 `ATT-b307766d-f0d7-4d0c-af98-1978f9d53762`；11:29:54.589 成功保存 437/437 单元，pending=0、最终保真通过，revision=4。七个模型响应均成功，最后 65 单元在原两次批量纠正内收敛，无本轮逐单元尾段纠正证据。随后 JobAid `ATT-07c04282-493c-4edf-888c-353fde2938a7` 于 11:33:28.089 因 `INITIAL_GATEWAY_HTTP_400` 结束；原生日志明确 `length/incomplete_result`、无可用结构化输出，客户端该请求未设置 completion token 上限。没有完整初始分析或 Review 成功。

真实事项 `MAT-26b208d0-1cc8-486e-a38f-7b9a99f74e7f` 已从上述任务正常创建；页面显示一份主要材料、尚无事项综合认识、材料尚未覆盖。c35 针对实测追加两项修正：M3 JobAid 单次申请 32000 completion tokens，全部 150 项输入和校验保持；统一消费者优先处理显式排队的 Review，使 Matter 不依赖 JobAid/Overall 完成，初始失败状态和 checkpoint 保留。166 项 Skill 测试通过，c35 正在私有打包安装。唯一 cron 当前暂停、consumer/invoke=0，consecutiveErrors=37 与历史保留。

旧 M3 `WI-ce687e29-b873-4f67-8aa3-281ec8050f8a` 的 437/437 翻译成功与后续 JobAid 300 秒失败、旧 DLI `WI-6d8ec7ab-69f6-40ba-9439-73c0fcc26980` 的 275/437 旧预算失败仍保持原记录，均不冒充完整初始分析或 Review 成功。

接下来完成 c35 安装读回，继续真实 Matter Review，并以正常新请求验证 M3/DLI 完整初始分析及连续 Review。原同范围开发、真实材料测试、origin 推送、技术发布及单 scope/唯一 cron 迁移授权持续有效；不向 GitHub 推送。既有 AGENTS/技能整理和用户临时产物独立保留。真实 RAG 与旧存储事故结论仍未完成，不额外建立恢复副本或多后端切换。

长期 owner：[WiseLink 前端与 v0.6 接续](codex://threads/01a06014-5282-7f90-91bf-12759224d211) 负责 W3/W4 前端；[R09 Engineering Matter 与跨事项目录](codex://threads/01a050bc-1d6c-7ce1-ba31-d4d0f781fdae) 负责 W2/W4 持久化；主控负责 W1、共享接口、执行链、集成及技术发布。设计收敛见[事项评估与阅读工作区设计评审](WISELINK_R10_MATTER_DESIGN_REVIEW_20260908.md)。

## 2026-09-08 新设计评审与下一批增量

用户本轮要求分析 9 月 8 日工程事项评估报告和 Matter Reading Workspace 设计，按需使用既有任务承担长期并行支线。已按 canonical HEAD `db1c65125d446098a72b68fd735d8db8cf8f8c60` 核对报告；其 `2bf3d482a` 基准为当前祖先，主要业务语义差距仍存在，后续 c32/c33 修复继续保留。结论与首批分工见 [事项评估与阅读工作区设计评审](WISELINK_R10_MATTER_DESIGN_REVIEW_20260908.md)。本轮为分析及接线方案，没有把附件中的主控指令当作新的实施/发布指令；原同范围授权继续有效。

下一批围绕“有依据、可核对、能够随纠正和材料更新的事项认识”交付，允许本轮评估完成而尚无实施决定。W1 阶段结果/合法前提、W2 事项工作内容、W3 首屏/依据及 W4 连续 Review 需在同一批增量接通。普通解释不换结果，纠正更新相关工作认识，新材料先标未覆盖；事项工作结果、成员文档结果、正式采用与工程师处置分别保存。静态包的虚构材料仅为界面/方法案例，不能替代真实业务验收。

已重新启用 [WiseLink 前端与 v0.6 接续](codex://threads/01a06014-5282-7f90-91bf-12759224d211) 负责 W3/W4 前端，以及 [R09 Engineering Matter 与跨事项目录](codex://threads/01a050bc-1d6c-7ce1-ba31-d4d0f781fdae) 负责 W2/W4 Host 持久化；两者已交付首轮只读差距评审，继续保留长期 owner。主控负责 W0/W1、shared/API、现有 Review 执行链与 Matter 范围接线及集成。W5/W6 按实际失败点/可用接口推进；旧存储支持不阻断本批，不新建重复目录、运行器、缓存或恢复系统。

下列 05:40 运行证据保持有效。c33 安装、M3 翻译成功与未完成的初始分析/Review 分别保留；新设计分析不修改模型、scope、唯一 cron 或旧失败历史。页面验证停在上次 Mac 锁定处，恢复时重新核对设备状态。第一条新业务验收采用本节的“无实施决定亦可完成评估”语义，M3/DLI 原有完整初始分析与连续 Review 目标继续保留。

## 2026-09-08 05:40 运行实证

用户于 9 月 7 日明确要求“优先把循环跑通、跑稳”，确认本次提交且后续同范围提交不再逐次授权；此前自动审查造成的提交阻塞已解除。授权覆盖同一真实 SB 交给已配置的 M3/DLI、正常新测试事项，以及原 Host 单事项 scope 和唯一消费者的对应迁移。验收仍是每个模型完整初始分析及同一事项内至少两轮连续 Review，其中包含纠正或补材料、真实来源读取和判断变化。所有产物只形成候选。c30 的输入精简/性能改动继续暂缓。

- **文档与任务：**资料库按 Document Management 的 family/文档/版本显示，最近任务独立展示。当前两个 787 测试事项共用 `document_1da16fdf399e24f094c32c8e` / `document_version_b91b79a4b966a02d8bab1cc0`，未重复创建文档版本。已上传 PDF 的精确复用修复 `302626f21` 保留 bytes、版本、来源和幂等校验，只允许已经完成精确链接的 acquisition 复用原版本。
- **最新技术发布：**Host release `7682886174448978881` 已 finished，提交 `0b5e4c6136f15503057914854a2fec254b34cfef`，错误日志为空。Skill-only 修复 `db1c65125d446098a72b68fd735d8db8cf8f8c60` 已仅推送 origin；官方安装轮次 `7682908825015536855` 已完成，唯一 Skill 目录实读 `wiselink-research-and-synthesize@r09.c33`，28/28 文件摘要一致、无缺失或多余文件。安装后 validation 135 项、initial consumer 7 项、Review consumer 3 项全部通过；本地发布器 2 项、lint 和实际 HTTP 实验通过。c33 尚无真实新业务请求验证，不把技术检查算作大循环验收。
- **长文预算修复：**DLI 前 8 轮均 HTTP 200/tool_calls，但旧 20 分钟预算在第 4 窗口纠正等待时耗尽，实际只通过 275/437 单元。c32 将全文/纠正共享预算设为 45 分钟、单响应上限 15 分钟，每轮通过确定性适配器续租原 Host attempt；续租失败停止后续模型请求与提交。原 30 分钟 lease、60 分钟 attempt deadline、两次纠正上限及最终保真校验保留。c31 的 M3 请求 32,000 输出额度及已核实模型条目配置保留；本次 M3 翻译的 9 轮均 HTTP 200/tool_calls，437/437 单元最终通过完整保真校验并成功提交候选。
- **客户端断连修复：**M3 后续 JobAid 在等待响应头约 300 秒时中断，早于该操作的 480 秒预算。已安装 OpenClaw 全局 agent 默认时限实际为 172800 秒；通用 timeout 文案不能证明默认 300 秒。源码与时间顺序指向 fetch 的独立 300 秒响应头上限先断开、Gateway 随后取消 agent。本地同一合成 HTTP 服务延迟 305500ms 返回，旧 fetch 在 301317ms 报 `UND_ERR_HEADERS_TIMEOUT`，c33 核心 HTTP 传输在 305508ms 完整收到 HTTP 200。c33 已接入 Initial 与 Review，沿用已有操作 AbortSignal，接收时限制 4 MiB；不修改全局超时、模型、会话、来源或正式采用边界。此实验使用本机 Node 24；云端 Node 22 的安装后短时 HTTP 和消费者测试另行通过。

| 模型与当前样本 | 实际状态 | 未完成部分 |
|---|---|---|
| DLI `WI-6d8ec7ab-69f6-40ba-9439-73c0fcc26980` | PDF 解析成功；翻译 `ATT-cc4a280e-469a-46f6-b1c8-52a4af1c6f75` 在 03:56:18.843 因旧总预算到期结束，275/437 单元通过，无完整候选。`CANCELLED_BY_REQUEST` 为消费者失败收尾，非用户取消。 | 旧失败保留；使用正常新请求验证 c33，再完成初始分析及两轮 Review。 |
| M3 `WI-ce687e29-b873-4f67-8aa3-281ec8050f8a` | 自然 tick 于 04:56:21 启动。翻译 `ATT-2f678555-4fac-4be2-beb3-630da31c554e` 于 05:03:36.264 成功提交 437/437 单元、438 个 SourceRef、零保真 finding，revision 4，候选 current。后续 JobAid `ATT-94cf4151-3a5e-4013-a948-232d5bea1d85` 于 05:08:41.764 因上述断连失败收尾；无 JobAid/Overall 候选。 | 翻译成功与后续失败分别保留；正常新请求验证完整初始分析，再在同一事项页面完成两轮连续 Review。 |

当前唯一 cron `8db789fa-4fe9-4e58-850c-97b4cd70eb46` 在核对空闲后原生暂停，c33 安装后仍 enabled=false、consumer/invoke 在途均为 0。Host online scope 与 argv 仍精确指向上述 M3 事项，间隔 60 秒、timeout 3600 秒。旧错误历史与退避保留；已失败 Host 阶段返回 `REQUIRES_ATTENTION`，下一 tick 不会自动重放。待正常新页面请求就绪，再按原授权迁移精确 scope/同一 cron 并恢复自然调度。未手动执行 driver/cron run，未重置历史或退避。

页面操作的当前恢复条件：CUA 再次报告 Mac 锁定且自动解锁失败，已请求用户解锁，尚未收到回复。独立可做的诊断、修复、origin 推送和官方安装已完成；新页面请求与连续 Review 需解锁后继续，不以管理接口或读取浏览器凭据代替真实页面身份。两轮 Review 的问题已准备：先使用“近期迁移 Windows 10”的明确测试假设，再撤回并改为继续 Windows 7，回读 SB 第 6/7 页，核对紧迫性变化及仍未知的飞机/软件构型。两事项 ReviewTurn 实读仍为空；不得将测试假设写成机队事实或进行正式采用。

## 9 月 7 日接管记录（历史时点）

2026-09-07 接管续查：Host release `7682594178899332332` 已 finished，对应 `408de9f17`，已安装 Skill c27。DLI 新事项 `WI-cb6a99bd-f336-48fe-bd95-a0e39e3311f1` 已生成 437 单元，但 attempt `ATT-f6f7c9e2-da51-4e2c-acbd-f4559d339e0c` 因 `TRANSLATION_RULE_PREFLIGHT_REJECTED` 取消，无候选。精确原始输出核查确认同时存在日期/中文相邻数字误判、漏译和跨单元串译；c28 与 Host 配套修正保真识别，在原模型、全文会话和总预算内对失败单元最多纠正两次，保留最终 Host 校验。40 项 Host 规则测试、16 项翻译服务测试、137 项 Skill 测试、lint 和生产构建通过；c28 已在唯一同名目录完成安装，27/27 文件摘要一致、installed 137/137 测试通过；Host release `7682816136916372431` 于 23:35:28 finished，对应 `45499e3a4`。尚无 c28 真实业务成功证据。旧失败保留，用正常新请求验证。下文 9 月 6 日状态保留为历史进度，以本段和后续新实证为准。

23:33 新 DLI 事项 `WI-d8c42bc9-d0d7-42dc-9a8b-6ba5e6193a99` 正常登记后解析失败，尚未调用模型。Trace `da008f3f52d44858ce169a903d3559a6` 精确定位为 professional package 的 FileService `preUpload` 无 HTTP 响应（`fetch failed`），并非 PDF 缺失；通用失败分类当时显示 `SOURCE_BINDING_FAILED`。后续 Host 修订只恢复同一不可变对象写入：丢失响应先查相同 bucket/path，存在则仍验实际字节，成功查得不存在才在 250ms 后以同一 bytes/path、`upsert:false` 重写一次；读回未知、HTTP/权限错误、字节不符或次数耗尽均停止。28 项定向测试、lint 与生产构建通过。修订 `aeddaf928` 随 release `7682818690430159859` 于 23:44:08 finished；旧事项/attempt 不重放，唯一 cron 保持暂停。新版的新 DLI 表单已准备，但提交再次被自动审批审查拒绝，理由是未接受原任务中的文件及 DLI 传输授权；已向用户请求在当前任务补充确认，未另换途径提交。新的自然 tick 验收待正常受理与范围迁移。R10 云文档四处状态已精确更新并读回，revision 2215；当前正文镜像同步。

## 本轮 Goal 修订目标

2026-09-06 20:05 用户同意改为**每任务选择模型**并继续，替代此前待确认的全局模型管理角色方案。创建事项时选一次，初始翻译/适用性/Job-Aid/综合评估继承；Review 默认继承，新回合可显式换模型，运行、同请求重试及恢复不换。沿用事项权限与已登记、获准模型，不新增角色/成员或处理密钥。页面、持久化和实际消费者已随 `aaac68ced` 发布；普通账户已实读目录，M3/DLI 根选择均已从页面保存。R10 云正文五处当前状态已更新并读回至 revision 2211，正文镜像同步。历史的全局角色待确认不再是当前前置条件。

9 月 6 日 23:00 历史状态：存储上传前只读传输重试及受理失败展示修订 `50a45b12e` 已于 22:48:42 发布，release `7682433168808643560` finished。M3 待跑事项 `WI-1c1e3f05-0807-4cd1-b43c-df4aea5e4be3` 解析成功、13 页原文可读；cron 与 Host scope 已对齐并启用，但调度器仍保留旧错误退避，实际 nextRunAt 为 23:09:44.307，此前较早时间没有实际启动。22:51:55 新 DLI 事项 `WI-214ded11-e5b5-4580-972d-0344da0f51c7` 受理及解析成功，根选择 `dli/gpt-5.6-sol`，等待空闲后迁移。22:31 M3 新事项的原存储失败保留、不重放。两模型完整初始分析及连续 Review 仍未完成；详见 [任务模型与实测记录](WL31_R10_TASK_MODEL_SELECTION_20260906.md)。

按修订后的 WiseLink R10 契约交付真实样本的页面自助连续评估：先修订飞书正文及第 12 节摘要并同步仓库；复用 Host、Hosted OpenClaw、Turn/ActionAttempt/MCP 与现有产物存储，接通页面自动执行、评估前共同上下文和 JobAid/Overall/Review 任务视图、稳定授权会话、按需关联读取及真实活动。工程师无需 Codex 手工启动 driver，即可连续提问、补材料、纠正方向，并看到候选回答和判断变化。真实 RAG 未接通据实展示，不阻断已有获准资料分析。第一批四项存储/读取改造已交付，继续与真实流程一起迭代；旧对象事故独立支持，不作为开发、联调或正常技术发布的前置条件。

2026-09-06 12:08 用户明确要求“更新 goal 并继续”；该页面自助连续评估目标尚未完成。完成依据是页面真实连续使用和代码/云文档状态一致，不能由安装、mock、主控手动执行或测试数量替代。当前任务已参考“更新 Astra 适配指令与技能”的完成结论，按需读取、按改动风险验证；其 AGENTS/技能修改独立保留，不混入本轮业务提交，也不改变 Hosted 产品模型。

容错策略按用户最新澄清区分阶段：**开发排障暂不自动重放失败回合；正式运行必须支持有界自动重试、可选资料失败后的明确跳过和单任务失败隔离。** 复用现有 ActionAttempt、幂等和恢复路径，记录退避、次数与终止原因；候选提交结果不确定先核对原结果，关键原文未读到、权限失效不得冒充成功。无需新建通用队列、第二业务状态机或无依据 gate。普通候选不自动确认 ReviewAction 或正式采用工程结论；既有认证、数据安全、SourceRef、版本/CAS、恢复和正式业务入口保持。

## 9 月 6 日阶段进度（历史时点）

| 步骤 | 状态 | 交付内容 |
| --- | --- | --- |
| 1. 契约与设计 | 任务级模型已发布，M3/DLI 根选择均已页面实测 | R10 revision 2211 五处当前状态已读回并同步镜像；任务级选择实际链随 `aaac68ced` 发布。M3 新事项正确保存选择但存储解析失败，DLI 新事项选择及解析成功。两模型生成验收待完成。 |
| 2. 页面到云端自动运行 | c24 自然 tick 已实际调用绑定的 M3，失败保留 | 新真实 SB 671 单元已读齐；M3 首轮网关 HTTP 400 / incomplete_result，未返回有效片段、无候选写回。原生 Gateway 的一次内部重试与 driver 的单次 HTTP 请求分别记账；旧 Turn 与全部失败 attempt/checkpoint 保留。 |
| 3. 连续调查与页面体验 | 数据库目录/快览已发布并实测 | 新目录返回当前账户 10 项资料；事故事项历史快览独立 HTTP 200，明确标注原文未在本次核验。两条实际 Trace 均无 FileService span；c22 跨轮会话能力继续保留。 |
| 4. 页面连续往返 | 787 原文可读、自动调度已启用；候选和 Review 待完成 | M3 待跑 `WI-1c1e3f05-0807-4cd1-b43c-df4aea5e4be3` 的 13 页 PDF 已实读，唯一 cron 与 Host scope 对齐；旧错误退避保留，23:00 尚无初始 attempt，实际计划 23:09:44.307。DLI `WI-214ded11-e5b5-4580-972d-0344da0f51c7` 已正常解析并保存根选择，等待后续空闲迁移。 |
| 5. 真实取证与可选资料隔离 | a7dad7e37 已发布，实际 Review 活动待验收 | 关联目标逐份隔离并保留原因；ActionAttempt 可空活动字段记录 Host 上下文准备与 SourceRef 解析，回合可回读并定位来源，不冒充模型阅读或正式采用。发布后数据库差异为空；见 [实现记录](WL31_R10_EVIDENCE_ACTIVITY_20260906.md)。 |

这些是实施顺序，不是逐级解锁的 gate。出现具体故障就定位原因、做必要普通测试，然后返回完整流程。

## 已完成的接线

1. **选中评估点。** selectedEvaluationItemId 已贯穿页面、请求、既有 Turn JSON、回读和 Agent Task/Context，历史无字段 Turn 继续兼容。本轮实测发现页面默认显示 GOV-008、原提交却为 GOV-001；前端源提交 70a74167c 已集成 8fd75218d，统一默认选择、分类切换、URL 和发送焦点。
2. **自动执行意图。** 新 append 在 Host 明确支持时携带 AUTOMATIC，持久化在现有 Turn JSON；普通保存与历史 Turn 不加入自动队列。范围来自已配置租户/事项，不是前端硬编码；automaticExecutionAvailable 不是实时健康状态。
3. **授权领取与运行。** get_pending_review_turn 复用现有 owner、官方身份映射与 exact WorkItem service scope；ActionAttempt 在模型上下文准备前持久化。实际 SQL 中 actor_id 别名冲突已以唯一 CTE 别名修复，没有降低 RLS。官方 Hosted 原生 command cron 每 60 秒读取唯一 Skill 安装路径，现已安装 c25 统一事项入口；13:34 auto 与 18:05 M3 的自然 tick 均实际运行 TRANSLATE，失败记录保留。c25 安装后尚无新任务生成验证；19:15 新 787 事项仅完成解析，不把安装或 PARSE_PDF 成功当作初始候选成功。
4. **页面真实状态。** 复用对话回读的 execution，显示状态、时间和失败原因。仅真实活动状态触发四秒只读刷新，终态和读取失败停止自动刷新；临时服务错误保留内容与草稿，权限失效清除不再可读内容。没有候选不等于正在运行。
5. **来源和材料。** 本次材料按需展开主文件、已保存输入/附件、正文引用目标和候选 SourceRef；此前 10 处真实引用聚合为 6 个目标，点击来源已定位受控 PDF 第 5 页；原文故障后不能复用该历史成功作为当前证明。c21 让模型先收到目录，再按问题请求相关片段，驱动委托既有 MCP 每批 100 项读取；同轮后续只发新 tool exchange，不重复发送整包。选入、读取、引用、采用仍分别表达。
6. **跨轮延续。** c22 的 Host 从现有 ReviewTurn/ActionAttempt 读取最近前序任务，同一租户/工程师/事项/讨论且前序成功、版本与新核实来源范围一致时延续。前序失败或范围改变则从当前 Turn 主键派生新会话并承接 Host 保存的讨论；查询沿用单语句 actor CTE 与现有 RLS。控制面 nativeSessionKey 仅传给 Gateway header，绑定 wiselink-engineering，不进入模型/浏览器；每轮来源引用仍需本轮实读。无新表、hash、gate 或原生配置变更。

## 本轮运行故障与修复

- Turn 15 已从页面创建并被原生 cron 自动领取，但模型前 sanitizer 把来源权威的重复 authority 别名视为运行权限字段。Host 2110b5c26 在模型投影中仅保留已有 sourceAuthority；原始快照、来源信息和权限检查不变。
- Turn 16 的实际焦点为 GOV-008；同样自动领取后，snapshotRef 内嵌 WorkItem ID 被模型边界拦截。云端离线扫描完整真实输入，只有这一处命中；Host 4edb3a376 将快照引用改为普通 UUID，事项/版本绑定仍存在快照正文。未恢复内容 hash，也未放开模型控制标识。
- 两次失败都保留了历史输入和错误状态；未调用模型、未保存候选、未正式采用。诊断只读云端原生运行记录与既有 checkpoint，离线依赖桩阻断全部远程业务调用，不重放已失败 Turn。
- 4edb3a376 与前端焦点修复已发布。无 criterion 参数页面创建的 Turn 17，数据库实读 selectedEvaluationItemId=GOV-008、executionRequested=true，证明默认展示与提交已一致。
- Turn 17 于 14:00:34 保存、14:01:23 在上下文准备阶段 FAILED；无模型调用、无候选。原文包在 13:57:33 仍能读回，随后存储元数据为 null；官方文件接口也返回空列表及 0 文件。原文读取失败连带阻止页面 fresh-read，旧 REQUESTED 不是仍在等待执行。详见 [存储异常记录](WL31_HOSTED_STORAGE_INCIDENT_20260905.md)。
- 前端主控已修复背景刷新误锁草稿，以及刷新失败时旧状态缺少明确提示；源提交 5d2e188c1，集成 d620b22c8。正常只读刷新可编辑草稿，真实写操作、防重复与权限失效清理保持。

## 继续工作的顺序

2026-09-06 10:25 用户最新明确要求：**继续业务开发，并行完成范围明确的存储与正常读取改进；旧对象故障转为独立支持事项，不再阻断开工、联调或正常技术发布。** 本节直接替换此前“先查清旧事故再继续”的顺序。已有备份与事故证据继续保留，不以新流程成功冒充事故解释，也不因调查改动审计配置。

既有 9 份历史 SourceArtifact 均不可读，其中 7 份原 PDF 对应 11 个 WorkItem；同桶新对象可读。已查 SQL 日志未发现故障窗口内源文件登记表变更证据，但不能替代文件服务内部记录。9 月 6 日 08:44 两条旧路径精确 GET 仍返回 `400000034`；本轮重新取得两条原始 Trace，细化了成功读取与元数据失败的边界，尚未取得删除、索引或归属权限变化的决定性证据。具体事实、可查范围及待平台回答的问题见 [存储异常记录](WL31_HOSTED_STORAGE_INCIDENT_20260905.md)。

1. **第一批四项与业务并行。** 存储内部保存 SDK 实返 locator，不扩散到 TaskEnvelope、不改历史 ref；`recordAcquisition` 源文件/受理登记采用短事务，上传读回留在事务外；数据库在已有权限范围内完成目录搜索、稳定分页和薄快览；同请求/任务复用一次授权绑定后的实际字节与解析结果，不复用授权决定、不把旧缓存当本轮主存储实读。
2. **前端与消费者一起接通。** 主控收敛 shared DTO 和真实调用链；前端继续目录、材料、连续讨论和判断变化；Host/Reader 优化存取与上下文；Agent 复用现有消费者与候选回写。目录与快览本来就不需要主包，不能实现成主包失败后的降级分支；讨论、执行状态直接读取业务记录。
3. **立即选择正常可读材料推进实流。** 优先已有获准非事故资料，无合适材料时正常受理一份真正不同的新真实 PDF。依次证明目录/快览可见、原文可读、初始候选、至少两轮连续追问，其中一轮纠正方向或补材料，并展示后续依据和判断变化。只使用已有自动执行链，不手工启动 driver，不重放失败回合，不重复已完成 InitialAnalysis。
4. **按真实读取行为验收并技术发布。** 目录/快览零主包下载；同次装配无不必要的重复下载/解析；单个来源失败不影响其他已授权事项；前端与后端实际接通。先看完整流程，再记录字节量、内存和阶段耗时，不预先承诺固定提速比，不等待所有结构改造完毕才联调。
5. **旧事故独立支持。** 保留原对象、原桶/路径、成功/失败 Trace 与备份，请平台回答底层对象、索引/归属/权限及删除清理生命周期。未指定支持入口或联系人，尚未发送；若平台需要才做获准隔离的同内容官方/真实SDK对照。不关闭事故、不切后端、不覆盖旧对象、不修改历史引用，也不把未读资料作为本轮依据。

存储机制评估同步采用这一顺序：保留 PostgreSQL＋文件服务，按访问方式组织数据；不是将全部 JSON 搬库，也不把全部 JSON 只能存文件当原则。独立恢复副本、同步双写、多后端切换和新分布式缓存不在实施范围。根因证据与正常路径改进分别交付，详见 [Host 存储设计评估](WL31_HOST_STORAGE_DESIGN_REVIEW_20260906.md)。

本轮增量已随 `1abbfefce` 发布：存储内部 `ordinary_artifact_locator`（发布前 dev→online 差异仅新增该表及必要约束/RLS，发布后差异为空）、受理登记短事务、owner-scoped 数据库目录/快览与前端真实接线、请求/任务读取复用。真实 SB fixture 的 Reader→JobAid 同次装配测得 1 次下载和 1 次完整 JSON 解析；目录/快览无 FileService 依赖。新资料自动初始分析的缺口以 c23 薄消费者补齐，由现有 Host projection/ActionAttempt 推导下一阶段，完成后进入原 Review 消费者，不新建业务状态机。Host/前端 12 suites / 132 tests、c23 本地 117 项及前后端生产构建通过；原有构建警告保留。本地测试与已发布、已安装、真实业务验收分别记账。

用户已明确授权使用 `/Volumes/SSD/LLM/WiseLink/Docs/uploads` 内不同真实 PDF，并允许将单事项执行 scope 切换到新样本及扩大多资料测试。首份选用 `737-46-1061_Original.pdf`（18 页）；其内容摘要与另外三份候选均未命中当前 DocumentVersion 表。保持文件内容不变、正常入口受理，不覆盖事故对象或历史引用。

## 协调与发布

### 评估包交接与响应效率（2026-09-05 新增）

用户要求作为当前 Goal 的体验要求纳入，不另设 Goal 或发布 gate。R10 §10.6 已明确：持久标识/版本与按需片段交接，前端首屏与重包处理分开，普通刷新不整页清空，身份/权限与实际版本仍从 Host 核实。目标为正常已登录网络下核心首屏 p50 ≤1 秒、p95 ≤2 秒、已加载事项切换 p95 ≤100 ms；当前未宣称达标。

Host 读取优化与前端 A+B 已随 `3903eb6c1` 发布；面板保活 C 集成为 `1d65d01cd`，c21 同轮按需取证及 c22 跨轮授权会话均已发布。主控继续轻量首屏/讨论状态、历史按需读取和真实活动；前端首屏拆包核查未找到足够收益的最小改动，本轮不集成实验性拆包、不重复已有 PDF 动态加载。依据、实现与实测范围见 [交接与响应记录](WL31_R10_CONTEXT_HANDOFF_PERFORMANCE.md)。

- [WiseLink R10 项目主控（2026-09-07 接管）](codex://threads/01a079d1-918d-7af1-a283-75968ec294ea)：shared DTO、Host/Skill、共同上下文、兼容集成、技术发布、实际运行与云文档。
- [WiseLink 前端与 v0.6 接续](codex://threads/01a06014-5282-7f90-91bf-12759224d211)：A、B、自动发送及焦点修复均已交付并集成。资料库稳定列表与快览、明确来源读取错误、日志脱敏的源提交 `a1f501c99` 已集成为 `11c92da4d`，主控补充分类兼容后随 `d1bd56b1d` 发布；本地组件验证与线上完整页面验收分别记录。后续按真实 Host read model 继续页面体验，不导入 prototype.js 或虚构业务脚本，不单独重复发布。
- 方法/解析：保持主文件结构、条件、例外；JobAid 正式来源据实际绑定派生。SOURCE_IDENTITY_MISMATCH 是固定占位，不应直接改成 MATCH。
- 数据支线：真实 RAG 薄适配、构型事件源及原有采用后重算；不建立全量知识镜像、复杂图谱或新的通用发布平台来阻断当前运行。

Host 与 Skill 可按兼容窗口分别发布；前后端仍为一个 Host App 发布单元。官方 Skill 为 r09.c25，兼容线 r09，Host 最低 r09.c10。此前 737 的自然 M3 失败及全部 Turn/request/attempt/checkpoint 保留；当前唯一 cron 已按原授权在空闲时迁至正常解析的 787 事项，Host 单事项 scope 对齐，50a45b12e 发布后已启用，只等待自然 tick。23:00 的实际 scheduler 仍保留一小时旧错误退避，没有新模型 attempt；未手工运行 driver 或 cron run。全局模型管理角色方案已经被用户确认的任务级选择替代。正式运行有界重试、可选失败跳过和任务隔离仍待实现，不将开发排障的不重放策略固化为生产停摆。

## 9 月 6 日已核实状态（历史时点）

当前分支 codex/wl31-r09-master-handoff-20260903；最新 Host release `7682433168808643560` 于 2026-09-06 22:48:42 finished，完整提交 `50a45b12e21d18b42d6efbe1613c836f2b6c1792`、error_logs 为空。此前任务级模型功能 `aaac68ced` 于 20:39:22 发布，线上可空模型列已核实；本次只读传输/失败展示修订发布前数据库差异为空。两个提交均以单一同名 refspec 非强制写入妙搭 origin 与获长期授权的 GitHub codex 分支；未改 main、标签、删除引用或强推。

官方 Hosted 最新安装轮次 `7682368307975376059` 回报 c25 Ready/Visible、26/26 源文件与 Publish Lite manifest 一致，installed 三文件测试 127 pass / 0 fail；只更新唯一 Skill，无模型、业务、配置或 cron 调用。私有存储的相对 download_url 不作可下载地址，安装使用官方 +file-sign 的临时绝对 HTTPS 链接。此前迁移管理轮次 `7682358935945530346` 在唯一 cron 空闲后禁用并仅修改 737 事项 argv；启用轮次 `7682360923508296677` 只启用同一任务，随后观察自然 tick，没有手工运行 driver。每 60 秒、timeout 1800 秒、原 cwd 和模型/profile/Gateway 保持；本轮尚未迁移至新 787 事项。原生 `runningAtMs` 排除重叠、不积压补跑的行为已由官方源码核实，无需另加锁/队列。

11:49 已登录 Chrome 实读新版目录与事故样本快览：

| 请求 | 实际证据 | 读取边界 |
| --- | --- | --- |
| `GET library/documents` | HTTP 200；10 项；Trace `3d0df3ac503b8f3c348dd7fbd483eb40`，app_server 48.23 ms，1 条 SQL | SQL 内含 tenant＋requested_by，稳定创建时间/事项主键排序；不选择完整 projection；0 FileService span |
| `GET work-items/.../quicklook` | HTTP 200；历史 777-34-0425 候选可见；Trace `9b77e9864c14f228ec7385cf2e5125f7`，app_server 125.53 ms，3 条 SQL | 两次现有权限绑定读取＋数据库薄摘要；0 FileService span；页面明确“已保存候选意见／原文未在本次核验” |

两条 Trace `is_break=false`。这是这两次请求的实际观测，不是 p50/p95 性能承诺，也不是旧原文恢复。此前 Chrome 原生文件选择器返回 `Not allowed`；用户随后明确要求改用内置浏览器，正常选择并上传 `737-46-1061_Original.pdf`，新事项和 18 页 PDF 已实际读回。未复制 Chrome 凭证、修改扩展权限或操作浏览器安全设置；此次成功不追认此前失败为成功。

历史页面已读到事项版本 11、Turn 17 FAILED 与历史候选；22:23 三个旧事项的真实请求仍报元数据错误，当时 bundle 为 73561af4d。9 月 6 日 00:45/08:44 旧主包精确 GET 仍失败；本轮没有旧原件恢复证据，40 张历史表的审计状态及恢复预览矛盾仍按事故记录保留。未改变审计设置、未执行恢复/删除，也未重放旧事项回合；获准新样本的初始执行见下节。执行计划按实际读回更新，云文档及正文镜像保留各自标注的同步时点。

样本 WI-2c1902db-c2cd-427d-b0d4-1f8f70fe6597，SB 777-34-0425，事项 revision 11；JobAid 150 项、124 项暂缺受控输入，Review ACTIVE。新增 Turn 15/16/17 尚未产生候选，历史 Turn 13/14 未采用。14:01 起原文存储读取异常，不能将之前的页面可用证据当成当前健康状态。

构建通过，仍有既有父工作树 tsconfig 扫描、模块类型和 chunk-size 警告；不静默压制、不为旁支扩大构建框架。检查与发布明细见本轮运行记录，不以重复测试次数替代运行。

## 全文翻译故障与模型核对（2026-09-06）

新事项在 13:34:19.986 被自然 tick 领取 TRANSLATE，13 个传输批次收齐 503 单元；attempt `AQ-6ae76040e7144305bf8a0cc812a267cb` 的全文模型运行 `chatcmpl_bf2c8379-4809-43ac-ba51-ce3b0b032f75` 最终为 `stopReason=length`、`incomplete_result`、HTTP 400，提交前取消，无候选、无事项版本推进。后续自然 tick 只报告需处理，未重发模型，原失败记录未改。初始阶段失败尚未接入页面进度，是待修正的可见性缺口。

容量只读轮次 `7682296925610396968` 核实当前 auto 声明 contextWindow 200000、maxTokens 8192；真实上游 token 用量和最终生效预算未知。原文 27107 字符，传输 JSON 157319 字节，其中机械标识约 94.6 KB，不能仅凭 length 断言输入上下文不足。用户要求默认整篇理解，仅为实际输入容量限制适配分段；已明确接受同一全文上下文下分次连续输出。辅助领域术语锚点、摘要和元数据作为带来源/版本的后续增量，当前不预建图谱或新表。

早期本地适配保持全文输入不变，模型仅输出单元序号与译文，由适配器恢复原来源绑定并执行既有完整性校验；新增安全输出形态观测，不记录原文、推理正文或凭证。当时两组普通测试 117 pass / 0 fail、尚未发布或安装；后续 c24 实跑及 c25 安装见下文。真实连续输出仍未完成，不能靠删除 checkpoint 或改旧记录强制重放。

模型清单只读轮次 `7682302157848349625` completed：本实例 Miaoda provider 实际登记且 available=true 的选项为 auto、多模态、Flash、MiniMax-M3；前三项声明输出 8192，M3 为 16000，均声明上下文 200000。该次只读核查时默认仍为 `miaoda/miaoda-model-auto`，图像默认为多模态，fallback 为空；工程 profile 无独立模型声明，沿用默认。其余 provider 的全局目录不算本实例可选；飞书 4 月公开名单也不等于此实例当前清单。该只读轮次未切换模型、未改配置/cron、未做推理测试；随后用户选择与实际变更如下。

用户于 2026-09-06 15:19 明确选择“就用自带的 m3”，不接入外部 GLM 或自定义 provider。官方管理轮次 `7682319105639271376` completed：预检后仅执行一次 `openclaw models set miaoda/minimax-m3`，默认文本模型读回一致；Gateway 在 15:25:13.661 记录 `config hot reload applied (agents.defaults.model.primary)`，无需重启。工程 profile 和其他无覆盖文本任务继承新默认；配置差异仅默认文本模型及官方 `meta.lastTouchedAt`，图像模型、fallback、凭据和连接未改，唯一 cron 仅只读检查。未添加外部密钥、运行模型探针/driver/cron run、重放失败翻译或修改 Host scope。R10 云文档与正文镜像已同步至 revision 2179；配置生效不是该 SB 已用 M3 完成的证据，初始候选、全文连续输出与两轮 Review 仍待验收。

用户随后确认全局默认仅影响后续新分析，并要求接入此前 DLI、两种模型均完成真实 SB 初始分析与连续 Review。用户已自行添加 DLI；只读管理轮次 7682344940369775802 completed，确认规范登记标识 dli/gpt-5.6-sol、available=true、上下文声明 200000、未显式声明输出上限。原生默认仍 M3、fallback 为空；未读密钥、未发工程生成请求，登记不算跑通。

c24 已安装并随 4d38faea7 发布 /settings/models 页面、Host 受控全局设置、新 ActionAttempt 的启动模型持久化，以及初始/Review 适配器的原生 x-openclaw-model 路由。选择只进入控制面及既有 inputHash，不进入模型业务输入；恢复不重选，失败不换 Provider。全文翻译紧凑输出并在必要时同会话续写，默认每个自然 tick 最多连续推进四个已就绪阶段；CAS 写回串行，独立背景资料最多四路读取。相关 7 suites / 52 tests、125 项 Skill 测试和生产构建通过；正常发布迁移后数据库差异为空，历史数据不改写。真实 M3 首轮失败，无候选；内置浏览器已实读初始状态变化、来源定位及模型设置只读页面。模型管理角色尚待用户确认，不擅自扩大现有角色权限。生产有界恢复重试仍需后续实现和实测，不以本次调度提速代替。

18:05 自然 tick 的 M3 翻译在首个输出前失败。只读诊断轮次 7682363592980958140 核实：driver 一次 HTTP 请求，原生 Gateway 内部空结果重试 1/1 后 length / incomplete_result，未返回工具参数或有效前缀，实际生效 maxTokens 与 reasoning 预算未知，不能伪报 token 用量。c24 事后续写无法承接不存在的前缀；c25 因此在生成前明确规划每次最多 96 个完整单元、约 6000 原文字符的输出工作窗，首次仍发送全文、后续同原生会话续写、不切输入或漏单元。c25 已安装，installed 127 项普通测试通过，尚待新任务真实验证；不重放原失败任务。19:15 新真实 787 事项的页面受理及解析成功已核实，无翻译 ActionAttempt。完整事实见 [c24 实跑与 c25 修订记录](WL31_R10_M3_OUTPUT_WINDOW_20260906.md)。

## 普通保存与正式采用边界

普通材料、讨论、工作判断和候选可以持久保存，不等同正式采用。保留既有认证、数据安全、SourceRef、事务、幂等、恢复和正式业务入口；不自动确认 ReviewAction、不正式采用工程结论、不代办正式审批。普通 Review 候选阶段不改变事项 revision/current/STALE，也不自动关闭讨论；初始候选的保存仍由 Host 按既有 CAS 规则推进 revision。
