# WiseLink R10 当前执行计划

更新日期：2026-09-05。依据：[R10 云文档](https://hv5zjf4j8yb.feishu.cn/docx/MA3fdjEycoISjHxptAqcsyxvn9b) 与 [当前正文镜像](WISELINK_R10_CURRENT.md)。本轮最新发布和页面证据见 [交接与响应记录](WL31_R10_CONTEXT_HANDOFF_PERFORMANCE.md)，此前视觉交付见 [Satin 运行记录](WL31_R10_SATIN_HOSTED_ROLLOUT_20260905.md)。

Git 同步授权更新：此前 Codex 分支推送不符合当时的 main-only 限制；用户现已明确长期允许向 `Liu-Xuan/WiseLink_Host` 非强制推送项目相关 `codex/*`，后续范围内不再重复询问。两个 main 的既有分叉（131 / 1）只暂停 main 镜像，不阻断已授权分支同步。见仓库根 `AGENTS.md` 与 [授权记录](WL31_GITHUB_SYNC_BOUNDARY_20260905.md)。c22 功能提交 `7a9b9c4b7` 已以单一同名 refspec 非强制推送两个远端，符合当前授权；未触碰 main、标签、删除或强推。

## 已设定的 Goal

让工程师在既有真实 SB 事项页面中提问、补材料、纠正方向后，由云端自动执行并基于共同上下文返回候选；连续追问承接讨论，展示实际取证、依据、未知及判断变化。全过程不依赖 Codex 手工启动 driver 或开发者电脑常驻。

Goal 当前为 ACTIVE，尚未完成。保存、安装、mock、自动领取或测试数量都不能代替页面连续使用与候选回写证据。当前优先修复真实流程暴露的问题，不增加缺乏具体失败场景的 gate、hash、冻结契约或第二业务状态机。

## 当前进度

| 步骤 | 状态 | 交付内容 |
| --- | --- | --- |
| 1. 契约与设计 | 已接入，持续同步 | R10 正文与 UI-N02 Silver / Carbon Satin 设计进入云文档及仓库；原型与生产证据分开。 |
| 2. 页面到云端自动运行 | 接线已运行，当前原文存储不可读 | Turn 执行意图、Host 授权领取、原生 command cron、实际 execution 回读和页面发送均已发布。Turn 15/16/17 均自动领取；15/16 的模型前兼容问题已修复，17 因原文包元数据不可读而 FAILED。 |
| 3. 连续调查与页面体验 | c22 授权跨轮会话及配套 Host 已发布 | 保留共同背景、原文故障下历史回读、面板保活和 c21 同轮按需取证；c22 使用 Host 当前授权、版本与来源范围决定会话延续。完整材料视图、逐材料使用记录及真实活动仍待完成，真实连续回合尚未验证。 |
| 4. 页面连续往返 | 验证中 | 真实资料展开与 PDF 来源定位已通过；完整自动候选、追问及判断变化说明仍待实际证明。 |

这些是实施顺序，不是逐级解锁的 gate。出现具体故障就定位原因、做必要普通测试，然后返回完整流程。

## 已完成的接线

1. **选中评估点。** selectedEvaluationItemId 已贯穿页面、请求、既有 Turn JSON、回读和 Agent Task/Context，历史无字段 Turn 继续兼容。本轮实测发现页面默认显示 GOV-008、原提交却为 GOV-001；前端源提交 70a74167c 已集成 8fd75218d，统一默认选择、分类切换、URL 和发送焦点。
2. **自动执行意图。** 新 append 在 Host 明确支持时携带 AUTOMATIC，持久化在现有 Turn JSON；普通保存与历史 Turn 不加入自动队列。范围来自已配置租户/事项，不是前端硬编码；automaticExecutionAvailable 不是实时健康状态。
3. **授权领取与运行。** get_pending_review_turn 复用现有 owner、官方身份映射与 exact WorkItem service scope；ActionAttempt 在模型上下文准备前持久化。实际 SQL 中 actor_id 别名冲突已以唯一 CTE 别名修复，没有降低 RLS。官方 Hosted 原生 command cron 仍每 60 秒读取唯一 Skill 安装路径，该 Skill 已更新 c22；此前 c19 空闲自然 tick 已有 IDLE/无模型调用实证，安装不冒充新业务运行。
4. **页面真实状态。** 复用对话回读的 execution，显示状态、时间和失败原因。仅真实活动状态触发四秒只读刷新，终态和读取失败停止自动刷新；临时服务错误保留内容与草稿，权限失效清除不再可读内容。没有候选不等于正在运行。
5. **来源和材料。** 本次材料按需展开主文件、已保存输入/附件、正文引用目标和候选 SourceRef；此前 10 处真实引用聚合为 6 个目标，点击来源已定位受控 PDF 第 5 页；原文故障后不能复用该历史成功作为当前证明。c21 让模型先收到目录，再按问题请求相关片段，驱动委托既有 MCP 每批 100 项读取；同轮后续只发新 tool exchange，不重复发送整包。选入、读取、引用、采用仍分别表达。
6. **跨轮延续。** c22 的 Host 从现有 ReviewTurn/ActionAttempt 读取最近前序任务，同一租户/工程师/事项/讨论且前序成功、版本与新核实来源范围一致时延续。前序失败或范围改变则从当前 Turn 主键派生新会话并承接 Host 保存的讨论；查询沿用单语句 actor CTE 与现有 RLS。控制面 nativeSessionKey 仅传给 Gateway header，绑定 wiselink-engineering，不进入模型/浏览器；每轮来源引用仍需本轮实读。无新表、hash、gate 或原生配置变更。

## 本轮运行故障与修复

- Turn 15 已从页面创建并被原生 cron 自动领取，但模型前 sanitizer 把来源权威的重复 authority 别名视为运行权限字段。Host 2110b5c26 在模型投影中仅保留已有 sourceAuthority；原始快照、来源信息和权限检查不变。
- Turn 16 的实际焦点为 GOV-008；同样自动领取后，snapshotRef 内嵌 WorkItem ID 被模型边界拦截。云端离线扫描完整真实输入，只有这一处命中；Host 4edb3a376 将快照引用改为普通 UUID，事项/版本绑定仍存在快照正文。未恢复内容 hash，也未放开模型控制标识。
- 两次失败都保留了历史输入和错误状态；未调用模型、未保存候选、未正式采用。诊断只读云端原生运行记录与既有 checkpoint，离线依赖桩阻断全部远程业务调用，不重放已失败 Turn。
- 4edb3a376 与前端焦点修复已发布。无 criterion 参数页面创建的 Turn 17，数据库实读 selectedEvaluationItemId=GOV-008、executionRequested=true，证明默认展示与提交已一致。
- Turn 17 于 14:00:34 保存、14:01:23 在上下文准备阶段 FAILED；无模型调用、无候选。原文包在 13:57:33 仍能读回，随后存储元数据为 null；官方文件接口也返回空列表及 0 文件。原文读取失败连带阻止页面 fresh-read，旧 REQUESTED 不是仍在等待执行。详见 [存储异常记录](WL31_HOSTED_STORAGE_INCIDENT_20260905.md)。
- 前端主控已修复背景刷新误锁草稿，以及刷新失败时旧状态缺少明确提示；源提交 5d2e188c1，集成 d620b22c8。正常只读刷新可编辑草稿，真实写操作、防重复与权限失效清理保持。

## 继续工作的顺序

2026-09-05 深夜修订：已逐路径确认 9 份历史 SourceArtifact 均不可读，其中 7 份原 PDF 对应 11 个 WorkItem；同桶新对象可读。旧 0.11 PT60 未执行清理，事故前后路径与元数据代码无变化，平台对象生命周期/索引/权限根因仍待确认。已完成相关记录的私有本机备份，尚未删除，不把备份、清理或错误展示修复当作原件恢复。先交付本轮资料库稳定列表＋快览及存储错误细分修复；并行推进实际 locator 持久化、受控恢复副本、完整产品上传与恢复验证。目录 GET/轻量快览不再依赖原包读取是下一条明确应用改造，不能靠最近访问缓存假装完整资料库。详细证据与清理关联范围见存储异常记录。

1. **已恢复历史内容与真实状态的只读使用。** 100709797 复用现有授权、ReviewConversation 与 ActionAttempt 回读，在原文临时 5xx 下独立显示历史讨论；发布后真实登录页已显示事项版本 11、Turn 17 FAILED、具体错误及历史 Turn 13/14 候选。原文错误与已保存内容分开，不提供新回合或正式采用操作，不伪造当前材料。后续不用重复创建业务回合验证这个读取修复。
2. **同时查清并恢复原桶旧文件可见性。** 官方用户接口、管理台和运行时均不能读取原文件，管理端仍查询原桶。c22 本轮对已有新测试文档再次完成元数据读取和 498 bytes 下载；旧 PDF 名称列表仍为空，22:23 三个旧事项均在原文元数据读取处报错。文件级原因与恢复仍需平台侧证据；不从片段重造或重新解析覆盖当前事项。恢复后从页面新回合完成自动候选与追问，不重放失败回合。
3. **接续共同上下文和持续调查。** c22 已官方安装且配套 Host 已发布，跨轮授权会话接线完成，待实际两个新页面回合验证；无需重复安装或先改 OpenClaw 配置。继续真实取证活动与理由摘要、逐材料使用记录。普通工作判断不要求正式采用；未接通 steering 时仍明确“下一轮指示”。
4. 按真实材料与上下文 read model 丰富前端：附件名称/版本/正文来源映射、逐材料选入/阅读/引用/采用回执。保留连续正文，不新增独立前端材料真值或另一套状态机。
5. 真实知识空间 RAG、构型事件源及既有采用后重算并行推进；未连接据实说明，继续已有材料分析，不用云盘搜索冒充 RAG。

## 协调与发布

### 评估包交接与响应效率（2026-09-05 新增）

用户要求作为当前 Goal 的体验要求纳入，不另设 Goal 或发布 gate。R10 §10.6 已明确：持久标识/版本与按需片段交接，前端首屏与重包处理分开，普通刷新不整页清空，身份/权限与实际版本仍从 Host 核实。目标为正常已登录网络下核心首屏 p50 ≤1 秒、p95 ≤2 秒、已加载事项切换 p95 ≤100 ms；当前未宣称达标。

Host 读取优化与前端 A+B 已随 `3903eb6c1` 发布；面板保活 C 集成为 `1d65d01cd`，c21 同轮按需取证及 c22 跨轮授权会话均已发布。主控继续轻量首屏/讨论状态、历史按需读取和真实活动；前端首屏拆包核查未找到足够收益的最小改动，本轮不集成实验性拆包、不重复已有 PDF 动态加载。依据、实现与实测范围见 [交接与响应记录](WL31_R10_CONTEXT_HANDOFF_PERFORMANCE.md)。

- 主控：shared DTO、Host/Skill、共同上下文、兼容集成、技术发布、实际运行与云文档。
- [WiseLink 前端与 v0.6 接续](codex://threads/01a06014-5282-7f90-91bf-12759224d211)：A、B、自动发送及焦点修复均已交付独立提交并集成。已派发资料库真实错误呈现的局部修复：明确原文元数据读取失败时，不再笼统提示连接/权限问题；只用既有 DTO/error helper，不扩展 API、权限或正式采用入口。该任务尚未交付/集成/发布；后续按真实 Host read model 继续页面体验，不导入 prototype.js 或虚构业务脚本，不单独重复发布。
- 方法/解析：保持主文件结构、条件、例外；JobAid 正式来源据实际绑定派生。SOURCE_IDENTITY_MISMATCH 是固定占位，不应直接改成 MATCH。
- 数据支线：真实 RAG 薄适配、构型事件源及原有采用后重算；不建立全量知识镜像、复杂图谱或新的通用发布平台来阻断当前运行。

Host 与 Skill 可按兼容窗口分别发布；前后端仍是一个 Host App 发布单元。当前 Skill 官方安装为 r09.c22，最低兼容 r09.c10；本次先安装 Skill、再发布配套 Host，双方均升级才启用跨轮延续。旧 Host 缺少新控制字段时 c22 明确记录逐轮隔离，旧 Skill 仍能使用新 Host 原有入口；不把字段存在当作能力已消费。历史 Turn 13/14 保留真实 c16/c17 记录，不因技术升级重写旧候选来源。

## 当前已核实状态

主分支 codex/wl31-r09-master-handoff-20260903；Host release 7682054671425031353 / 7a9b9c4b7 已 finished，error_logs 为空，功能代码已按现行授权非强制同步 origin 与 github。c22 安装轮次 7682052334020758506 completed，唯一同名/Ready/Visible，23/23 源文件一致，installed validation 107/107、consumer 3/3；主控 Host 4 suites / 51 tests、server type-check 和前后端生产构建通过。均不代替业务跨轮验收。原生界面仍可读历史讨论、事项版本 11 与 Turn 17 FAILED，刷新受窗口切换中断；22:23 三个旧事项的真实请求仍报文件元数据错误，前端请求仍来自既有 73561af4d bundle。c22 本轮新测试文档元数据及 498 bytes 下载成功，未再次与最初文件逐字节比较。无数据库迁移、无新业务回合。R10 当前状态按本次发布与复查局部同步；具体云文档 revision 见正文镜像页首。

样本 WI-2c1902db-c2cd-427d-b0d4-1f8f70fe6597，SB 777-34-0425，事项 revision 11；JobAid 150 项、124 项暂缺受控输入，Review ACTIVE。新增 Turn 15/16/17 尚未产生候选，历史 Turn 13/14 未采用。14:01 起原文存储读取异常，不能将之前的页面可用证据当成当前健康状态。

构建通过，仍有既有父工作树 tsconfig 扫描、模块类型和 chunk-size 警告；不静默压制、不为旁支扩大构建框架。检查与发布明细见本轮运行记录，不以重复测试次数替代运行。

## 普通保存与正式采用边界

普通材料、讨论、工作判断和候选可以持久保存，不等同正式采用。保留既有认证、数据安全、SourceRef、事务、幂等、恢复和正式业务入口；不自动确认 ReviewAction、不正式采用工程结论、不代办正式审批。候选阶段不改变事项 revision/current/STALE，也不自动关闭讨论。
