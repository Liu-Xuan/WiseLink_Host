# WiseLink 当前整合实施表

2026-09-20。本表承接 [R10 当前规则](WISELINK_R10_CURRENT.md)、[R10 执行计划](WISELINK_R10_EXECUTION_PLAN.md)和用户最新 Goal。它按当前代码安排批次，不把静态参考、本地检查、Git 同步和技术发布写成线上业务验收。历史计划中与用户后来明确指定的设计冲突之处，以本表开头的优先级为准。

## 依据与当前恢复点

- 工程态势以 `/Users/liuxuan/Downloads/WiseLink_工程态势_信息聚合与协同评估.html` 为最高视觉和交互依据：信息聚合内环、评估辅助外环、中心智能体、宏观/聚焦、来源下钻、动态光效和演示。既有 Trinity 画布只作可选择复用的实现材料。
- 其他全部页面以 `/Users/liuxuan/Downloads/WiseLink_Frontend_Suite_20260917/` 的实际静态页面、组件、样式和截图为基准。真实身份、来源、正式版本、工作保存、CAS、对象授权和正式采用仍由 Host 决定。样例只在显式开发预览中出现。
- 本表编写时的实现恢复点为 `codex/0-11` 的 `d7b074447088c4594e0e1fdd67e35ef4e01e97a8`，父提交 `957f327cd0a8a24b062baacc5744d96aae136498`；两端同名分支已分别回读相同 SHA。`8d186d5b2` 的本地身份边界修复在祖先链；本轮知识读取 `957f327cd` 和资料库默认选择 `d7b074447` 已提交。本地资料库相关 19 项测试、client typecheck 和局部 lint 通过。四个原有未跟踪调试文件仍在工作树，未提交。
- 这些是代码与本地验证证据。当前提交尚无精确 Hosted 发布、真实登录功能链、性能或视觉验收回执；不沿用历史 release 代替本轮结论。

## 执行批次

| 批次 | 可审查的完成物 | 验证重点 |
| --- | --- | --- |
| 0. 正式入口与安全基线 | 保留已修的身份/对象授权；将正式 `/reader/:documentId`、`/version-comparison/:documentId` 的构造数据入口迁至隔离预览，接回现有真实文档版本/换版读取和准确参数；核对路由、身份注入与 CSRF，不用本地模拟权限 | 未知 ID、撤权、版本缺失明确失败；正式入口绝不返回 AMM 等构造内容；本地真实路由与 Hosted 分别核对 |
| 1. 简明阅读链 | 资料库首屏主题和认识不回退成长任务提问；正式 Wiki 与工程知识按同一保存工作展示问题、关键条件、当前认识和覆盖范围；Reader 连续正文、目录、准确原文及返回按 Suite 布局完善 | 资料库→知识/Wiki→精确原文→返回；有问题正文无综合、STALE、历史工作、来源撤权各保留准确含义 |
| 2. 完整图谱与时间 | 在已有 Cytoscape 和真实 typed 读取上还原 Suite 三栏、分组卡、中心圆环、配色、材质、连线、四视角、筛选、相机、手机活动面；时间轴保留真实事件身份、预计历史和时间窗 | 1672/1440/1024/390 同条件截图及交互；真实关系与样例分开；同一事件往返、准确来源与选中状态恢复 |
| 3. 其余页面与态势 | 单文件工程态势改造成生产 React 双环；自身换版区分同 family 正式改版与参考变化；问题分析、复核、工作进展、导览和共享外壳逐页对照 Suite | 完整视觉、交互、主题、动效及 reduced-motion；页面无伪业务状态或无意义空态；正式来源与实际执行分别核对 |
| 4. 真实业务与性能闭合 | 延续 R10 当前工作更正、Overall/知识同源、连续保存与恢复、准确综合、跨事项引用、获准后台来源及真实长 SB；稳定后测部署环境冷/暖读取并修实测瓶颈 | 正式换版与参考变化不同后续；无重跑有效中文、无虚构结论；常用页面以秒级有用内容为目标，记录实际时延分布 |

批次 0 和 1 优先关闭正式入口风险和最常用阅读链；图谱为独立完整交付，不用三栏骨架或编译通过替代。业务后端可在不覆盖共享文件的范围与前端并行，发布组合串行。每批先核对在途与当前 HEAD，明确文件所有权及旧成果，再进行最小实现、针对性测试、同条件视觉对照和独立审查；有新失败才扩大 gate。

## 每批交付与接受

记录父提交、准确文件清单、测试退出码、参考/实现截图的内部 viewport、DPR、字体、主题、效果、数据、选择和时间条件，以及未实现项。Git 只对同名 `codex/*` 分支分别向 `origin` 和 `github` 普通快进并读回 SHA；提交前排除凭据、调试文件和平台夹带修改。技术发布必须指向准确接受组合，另记 `release_id`、最终状态、commit 和错误日志。线上验收以真实登录身份和受权对象走通资料库、图谱/时间轴、知识正文、精确原文、返回恢复及保存/来源版本边界；本地 fixture、静态包、Git 和技术发布均不能代替该证据。
