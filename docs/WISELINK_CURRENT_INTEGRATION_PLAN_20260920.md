# WiseLink 当前整合实施表

2026-09-20。本表承接 [R10 当前规则](WISELINK_R10_CURRENT.md)、[R10 执行计划](WISELINK_R10_EXECUTION_PLAN.md)和用户最新 Goal。它按当前代码安排批次，不把静态参考、本地检查、Git 同步和技术发布写成线上业务验收。历史计划中与用户后来明确指定的设计冲突之处，以本表开头的优先级为准。

## 依据与当前恢复点

- 工程态势以 `/Users/liuxuan/Downloads/WiseLink_工程态势_信息聚合与协同评估.html` 为最高视觉和交互依据：信息聚合内环、评估辅助外环、中心智能体、宏观/聚焦、来源下钻、动态光效和演示。既有 Trinity 画布只作可选择复用的实现材料。
- 其他全部页面以 `/Users/liuxuan/Downloads/WiseLink_Frontend_Suite_20260917/` 的实际静态页面、组件、样式和截图为基准。真实身份、来源、正式版本、工作保存、CAS、对象授权和正式采用仍由 Host 决定。样例只在显式开发预览中出现。
- 当前实现恢复点为 `codex/0-11` 的 `1031e3caa00aa142b2e9c90d9d71fde8b7c241b8`，父提交 `7ecc01426`；`origin/codex/0-11` 与 `github/codex/0-11` 已分别回读相同 SHA。祖先链包含身份边界修复 `8d186d5b2`、知识读取修复 `957f327cd`、资料库真实默认选择 `d7b074447` 和实施表 `7ecc01426`；`1031e3caa` 已将正式 Reader/换版入口迁回真实授权读取链，并把 Suite 构造页限制在 `dev-preview`。
- 当前本地证据为相关路由与阅读测试 71 项通过、client typecheck 和 lint 通过。旧 `single-app-workspace.spec.ts` 仍有 1 项与当前外壳不符的既存断言，须在改动共享外壳时一并校正，不能据此否定已通过的局部范围。四个原有未跟踪调试文件 `.claude/launch.json`、`query-work-items.cjs`、`query-work-items.mjs`、`scripts/create-test-work-item.mjs` 继续隔离，不提交也不删除。
- 以上只是代码恢复点与本地检查。单独启动 Vite 时，应用根路径和深链接由服务器返回空 404；该结果只说明启动链不完整，不能推断 React 路由、数据库或 Drizzle 故障。当前提交尚无官方完整本地启动链、精确 Hosted 发布、真实登录功能链、性能或视觉验收回执；不沿用历史 release 代替本轮结论。

## 执行批次

| 批次 | 可审查的完成物 | 验证重点 |
| --- | --- | --- |
| 0. 正式入口与安全基线（代码已完成，运行证据待补） | 已保留身份/对象授权并完成正式 Reader/换版入口去样例化；下一步只补官方完整本地启动链的路由、身份注入和 CSRF 证据，再形成精确 Hosted 候选，不返工已通过修复 | 未知 ID、撤权、版本缺失明确失败；正式入口绝不返回 AMM 等构造内容；明确区分页面 404、API 不存在、身份 503、对象授权 404 与来源缺失 |
| 1. 简明连续阅读链（当前批次） | 按 Suite 把真实 Reader 完善为连续文档阅读：正文保留原文结构并去除重复页眉页脚干扰；页级来源定位落在文字或段落交互上；目录、五种阅读模式、分栏、全屏、返回恢复与真实来源保持一致。资料库、Wiki、知识继续使用同一保存工作，首屏先给主题、问题、关键条件与当前认识 | 先用显式 fixture 做同条件视觉载具，再走正式 typed API；资料库→知识/Wiki→准确原文→返回；有问题正文无综合、STALE、历史工作、来源撤权各保留准确含义；桌面与 390 宽度均无无意义空态 |
| 2. 完整图谱与时间 | 在已有 Cytoscape 和真实 typed 读取上还原 Suite 三栏、分组卡、中心圆环、配色、材质、连线、四视角、筛选、相机、手机活动面；时间轴保留真实事件身份、预计历史和时间窗 | 1672/1440/1024/390 同条件截图及交互；真实关系与样例分开；同一事件往返、准确来源与选中状态恢复 |
| 3. 其余页面与态势 | 单文件工程态势改造成生产 React 双环；自身换版区分同 family 正式改版与参考变化；问题分析、复核、工作进展、导览和共享外壳逐页对照 Suite | 完整视觉、交互、主题、动效及 reduced-motion；页面无伪业务状态或无意义空态；正式来源与实际执行分别核对 |
| 4. 真实业务与性能闭合 | 延续 R10 当前工作更正、Overall/知识同源、连续保存与恢复、准确综合、跨事项引用、获准后台来源及真实长 SB；稳定后测部署环境冷/暖读取并修实测瓶颈 | 正式换版与参考变化不同后续；无重跑有效中文、无虚构结论；常用页面以秒级有用内容为目标，记录实际时延分布 |

批次 0 的代码风险已经关闭，不能因完整本地启动链尚未恢复而回退到本地模拟权限或生产 mock。当前先完成批次 1 的 Reader 真实组件和视觉纵切，同时修通官方本地启动链；两者完成后再进入完整图谱与时间。图谱为独立完整交付，不用三栏骨架或编译通过替代。业务后端可在不覆盖共享文件的范围与前端并行，发布组合串行。每批先核对在途与当前 HEAD，明确文件所有权及旧成果，再进行最小实现、针对性测试、同条件视觉对照和独立审查；有新失败才扩大 gate。

## 当前 Reader 批次的开发顺序

1. 以 Suite `reader.html`、React 源组件、完整 CSS 和参考截图建立逐区差距表，分别标注纯视觉差距、真实读取差距和来源/权限边界；不从构造页复制业务数据到正式入口。
2. 在现有 `DocumentVersionReadingPage`、`DocumentSourceReadingWorkspace`、`DocumentOriginalReader` 和阅读样式上选择性实现连续正文、目录、阅读模式、分栏及全屏。复用现有授权读取和精确版本参数，不新建平行 API。
3. 将“已读取页数”“不代表工程结论”等必要边界收敛到不打断正文的位置；页级准确定位改为段落或正文链接。去除中间页重复页眉页脚时保留真实页码、来源和可追溯关系。
4. 用显式 `dev-preview` fixture 固定视觉状态，先完成 1672/1440，再检查 1024/390、深浅主题、动效和 `reduced-motion`；fixture 只证明视觉和交互，不证明正式读取。
5. 使用官方完整本地启动链验证 base path、身份注入、CSRF、typed API、撤权和返回恢复。只有同一精确提交通过后才发布 Hosted 候选，再以真实登录身份做业务链与性能测量。

首批文件范围收敛为真实 `DocumentVersionReadingPage`、`DocumentSourceReadingWorkspace`、`DocumentOriginalReader` 和 `document-version-reading.css`。实现独立控制栏、可收目录、五种阅读模式、两张阅读面、桌面分栏和手机单面切换；中文模式复用现有语义读取的 `translation` 能力，仅中文和中英模式按需请求译文。保留 documentVersionId、pinned parseRun、epoch/AbortController、401/403/404 清空旧内容、受控原件 URL 生命周期和准确返回上下文。

首批验收至少覆盖 1672x1000、1440x900 和 390x844：目录收起后正文实际扩宽，分栏拖动和键盘调整有效，五模式均可到达，默认原文加原件不请求译文，历史 parseRun 切换模式后不漂移，来源定位使用真实 unitId、SourceRef 和保存的 location，全屏失败有可见反馈且退出不丢返回上下文。双语独立滚动面的锚点同步必须基于真实语义 block/anchor；PDF 区域高亮必须基于受控 PDF 渲染面和真实 boxes。二者未完成前明确列为后续子批次，不用数组索引、滚动百分比、公开 PDF 或演示图片伪装完成。

## 每批交付与接受

记录父提交、准确文件清单、测试退出码、参考/实现截图的内部 viewport、DPR、字体、主题、效果、数据、选择和时间条件，以及未实现项。Git 只对同名 `codex/*` 分支分别向 `origin` 和 `github` 普通快进并读回 SHA；提交前排除凭据、调试文件和平台夹带修改。技术发布必须指向准确接受组合，另记 `release_id`、最终状态、commit 和错误日志。线上验收以真实登录身份和受权对象走通资料库、图谱/时间轴、知识正文、精确原文、返回恢复及保存/来源版本边界；本地 fixture、静态包、Git 和技术发布均不能代替该证据。
