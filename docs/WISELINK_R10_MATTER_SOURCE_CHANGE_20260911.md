# R10 v5 来源变化续接运行记录 · 2026-09-11

## 本次输入与授权

用户明确授权将本地 `787-FTD-45-25001_Doc_05082026.pdf` 上传到现有 Host，作为当前 Matter 的 CMCF 构型核查背景，并授权同一目标、目的地和范围内的后续类似操作。沿用已配置的官方 Hosted profile；该授权不改变正式采用、权限扩大等边界。

正常页面受理生成 `WI-990d6e76-78d4-440a-a419-1b2e37b94ac9`，08:23:34 显示解析完成。随后通过资料库关联到 `MAT-26b208d0-1cc8-486e-a38f-7b9a99f74e7f`。页面读回 3 份材料，FTD 为关联来源且尚未纳入核查，原工作修订 5 保留。没有重复上传、重做既有初评或发送业务聊天更新。

FTD 的正文修订日期是 2026-03-23，文件生成日期为 2026-05-08；页面的生成日期不替代正文版次。CMCF 报告问题与 SB 的 MCDF 软件兼容性问题分别保留，是否构成有关背景由实际调查说明。

## 首次自然运行与失败

c77 已安装并逐文件核对，装后 267 项测试通过。原 Matter job `efc2b938-2bab-4f6d-ab8d-14ca3de9fa70` 在来源变化后自然触发：08:25、08:27、08:29、08:35 均失败，未生成 Matter ActionAttempt 或新工作。第三次收到 `REVIEW_HOST_MCP_TOOL_FAILED:next_matter_assessment`；其余客户端约 60 秒后报告通用失败。官方 MCP SDK 默认请求超时为 60 秒。

Host 同批三次工具请求于 08:30:12 收束，耗时约 284348、155249、25502 毫秒。原始异常没有保存在客户端，不能仅凭超时或安全错误码断言 SQLSTATE 或唯一根因。旧 WorkItem job 同期短暂失败后恢复到原 `REQUIRES_ATTENTION` 状态。

## 事务连接缺陷修复

检查实际代码发现，Matter reservation 在数据库事务内调用模型设置读取，后者另用全局连接。使用真实模型 Service/Repository 和单连接本地 PostgreSQL 复现：外层事务占用唯一连接，模型读取等待第二条连接，无法完成。

提交 `3a127223fe94361ad665d27952abe4a1d8ceb142` 使模型设置读取复用 reservation 的现有事务，保留租户条件、模型目录校验及原设置选择。未变更模型、服务范围或数据库结构。Matter 集成测试的两处模型 mock 改用真实仓库，覆盖单连接下的 reservation、后续来源变化、保存、CAS、取消和授权。

验证：4 组真实 PostgreSQL 流程测试、10 项模型设置单元测试、服务端类型检查和生产构建通过。仅推送飞书 `origin`；发布 `7684070300170390748` 已完成并读回精确提交 `3a127223f`，无发布错误。

## 后续运行

修复后通过官方命令恢复同一个 job，保留原参数、身份、历史及一分钟计划；未 `cron run`、未手工运行 driver、未另建分析请求。官方 edit 和 disable/enable 并未提前解除旧退避，最终等待原退避自然到期。

08:51:16 创建并领取 `ATT-d381ef53-7701-4dcf-b462-bd75cd873235`（`AQ-22cadc7790ed4604ae4045fd207be257`）。数据库读回：真实 subject 为 `ENGINEERING_MATTER`、`work_item_id` 为空；SOURCE_CHANGE 仅列新增 FTD 的输入 ID；基于 Matter 修订 3、工作修订 5，精确旧工作引用 `MWREV-03dd5584-6c3f-478d-8138-97f680ed8e2b`，3 份输入，沿用 `miaoda/minimax-m3`。

08:53:07 消费者以通用错误退出；Host 仍为 RUNNING，最后心跳 08:51:58，租约到 09:21:58，未见读取活动或新保存工作。原生日志存在一次模型请求，但客户端只保存 `model.started`、没有 `model.result`；生成结果需进一步核对同一次原生会话，不能重放或声称成功。观察方此前从旧目录检查推断“零 attempt”的结论已被数据库及 CLAIM/HEARTBEAT 日志纠正。

后续代码检查又发现原文 Reader 在 Matter 事务内执行自己的数据库与文件访问。将其移到事务外，读取前后分别执行当前授权和租约校验，再用短事务记录实际读取。单连接真实 PostgreSQL 测试覆盖读取期间的同池查询、心跳，以及读取途中租约失效、成员权限撤回后不写入阅读回执；4 组流程测试通过，类型检查和生产构建通过。此修复针对明确的连接占用缺陷，不能代替本次原生模型结果的取证与恢复。本记录尚不声称完整来源变化续接成功。

发布 `7684077185384827882` 已 finished，精确提交为 `c78a48c812534133e973971e71c14474033ed6fe`，无发布错误。

## 原生返回核对与逐轮恢复

官方环境只读核对确认，同一 AQ 对应原生 session `e01817da-9e78-4382-ae5d-18499ab2667c`，sessionKey 尾部为 `initial:aq-22cadc7790ed4604ae4045fd207be257:1`。08:51:57 模型完成、session success，返回 `return_wiselink_assessment_step` 的 `READ_SOURCES`：SB 第 6 页与新增 FTD 第 1 页。完整参数存在原生消息存储本体；trajectory 的 arguments 被诊断脱敏截断，不能作为完整原始参数。未输出推理和原文，未将诊断摘要手写进 checkpoint。

此前“没有 model.output-shape 文件就没有模型返回”的推断不成立：Matter consumer 没有传入该观测回调。模型返回与随后原文读取超时的时间线吻合，但原文 Reader 的具体线上异常仍不能仅由耗时推出。

当前正在实现同一 attempt 内的逐轮恢复：完整模型响应在读/存意图执行前持久化；实际消息文本与原生会话身份跨进程保留，Host 重新发放的租约只用于当前授权调用；保存使用同一请求号并先回读。未知模型响应仍停止，完整结果丢失响应仍沿用原有精确 FINISH 恢复。联测覆盖读原文失败后续接、保存响应与回读同时失败、未知模型响应、输入/会话错绑，以及真实消费者换租约后不重复第一轮生成。旧版本在途请求仍需由原生完整返回进行受控恢复，尚未安装这批 Skill 改动，不能据此声称线上续接已成功。

后续 c78（提交 `cc26a119c6dfc9b9b5e9dbf7ce88ea9eb6b1db6c`）已通过官方安装，44/44 文件匹配、275 项测试通过。真实原生返回只读取证核验成功。因下一次退避晚于任务截止时间，09:35:10 用官方命令单次触发原 job；正常 CLAIM 发放第 2 代租约，恢复原来的 READ_SOURCES，未重复首轮生成。两页原文读取都已在 Host 的活动中保存，随后实际生成第 2—4 轮 SAVE_WORK 候选。

09:39:16 保存以 `JOBAID_SOURCE_NOT_DELIVERED` 拒绝。两个 save 文件仅保存请求号，不能证明工作已保存；Host 当前只有两条来源读取活动、没有本次工作保存。调度退避因错误增加到 6 次，下次 10:39:16，晚于原任务 09:51:16 截止；不得修改旧截止时间或租约绕过终态。

进一步核对确认：旧工作修订 5 尚无 problemWork，完整的旧阅读结果及 12 条证据通过 sealed task 的 previousWork.legacySummary 发给模型，但 builder 的 sourceCatalog/initiallyDeliveredRefs 和 SAVE_WORK 的 registry/readRefs 均遗漏了它们。新原文的两条 DOCUMENT_VERSION 引用则都有实际读取回执，静态 invocation 中的 NOT_READ_THIS_ATTEMPT 不能表示后续执行状态。

修复同时覆盖新任务与已发出的旧 v2 任务：新 builder 登记实际发送的 legacySummary 证据；旧任务保存仅补用其原始封存输入内已交付的 legacySummary.evidence，保留工程师陈述等原来源性质，不把未交付来源自动视为已读。真实 PostgreSQL 测试重建旧工作结构及旧版遗漏目录的任务，仍拒绝未读的新页，读取后正常保存、回读、重放和完成；4 组流程、服务端类型检查和生产构建通过。原超时请求与候选响应保留，跨请求恢复仍需正常入口和明确绑定验证。

## 失败候选的有界续接实现

原 SOURCE_CHANGE 任务已生成完整候选，但保存因旧工作证据注册缺失被拒，原任务期限随后到期。新增既有 begin 接口的 recoveryAttemptRef：Host 在事务内核对原 actor、Matter/工作修订、精确旧工作和当前授权输入；仅 FAILED/TIMED_OUT 可续接，并保留旧任务历史及期限。新任务继承原捕获模型和实际来源读取回执。Skill c79 验证原检查点绑定后，以新任务的正常保存入口提交原始完整候选，再续接完成步骤。

本地真实 PostgreSQL 流程覆盖超时转终态、不同新 attempt、原期限不变、实际已读来源复用、正常保存和完成、取消拒绝、幂等及工作基础变化拒绝；MCP 参数与消费者候选复用测试通过。此处记录的是实现与本地验证，真实业务保存和完成尚待发布后验收。

## c79 真实恢复结果与下一修复

Host 发布 `7684102639270136796` 已完成，精确提交 `880636c4650400094ee56dfd7f3664114cc9e801`；官方 Skill c79 安装 46/46 文件匹配，277 项测试通过，原第 4 轮候选只读绑定核验成功。新恢复任务 `AQ-050ee334467a4ee9a9bb71b30888e7ec` 按固定请求号正常登记，原 `AQ-22cadc7790ed4604ae4045fd207be257` 保持 TIMED_OUT 和原期限。

原 job 的单次官方执行 `manual:efc2b938-2bab-4f6d-ab8d-14ca3de9fa70:1789096014468:2` 领取了新任务。第 1 轮原样提交旧完整候选，被 Host 以 `JOBAID_LIKELIHOOD_BUSINESS_EVIDENCE_REQUIRED` 拒绝：维护中断风险的“不大可能”仅引用 ENGINEER_STATEMENT。第 2 轮 HTTP 200、stop，返回完整纯文本而无函数调用，驱动于 11:08:47 退出 `JOBAID_MODEL_OUTPUT_FUNCTION_INVALID`。工作仍为修订 5，没有新保存或完成。消费者退出不等于 Host 任务已经终止；原期限、租约和检查点保留。

c80 增加已知完成纯文本响应的有界格式纠正，复用已记录响应、原业务拒绝和候选，不重新生成旧轮次；补充评级字段及来源性质的明确反馈，保持 Host 业务依据校验。测试覆盖此纠正期间中断后续接、原 SAVE 不重复、评级 null 仍保留风险情景、两次纠正上限，以及截断和非预期工具不进入修复。真实 c80 续接尚待运行核验。

## c80 续接计时缺陷与 c81

c80 官方安装 46/46 文件一致、279 项测试通过。一次续接 `manual:efc2b938-2bab-4f6d-ab8d-14ca3de9fa70:1789098100468:3` 于 11:41:40 接受，领取 generation 2 后约 4 秒因 `JOBAID_MODEL_BUDGET_EXHAUSTED` 退出，未调用新模型或保存新工作。原因是原 30 分钟 wall-clock 计时把 11:06:57 之后的停机维护全部计入；Host 任务截止仍为 12:03:38。主控等待完整租约并非必要：现有 CLAIM 已允许同一 principal 读回自有有效租约。

c81 对有封存 deadline 的 Matter 改为按原检查点中每轮实际模型执行时间累计 30 分钟预算，保持 Host 整体截止、原始时间记录、模型、输入、轮次和纠正计数。WorkItem 路径不变。测试覆盖维护后复用旧响应、模型累计耗尽、Host 绝对截止和异常时间记录；真实后续保存仍待核验。
