# WiseLink R10 本次修订与交接说明

**日期：2026-09-16。**
本说明记录本次对两份上传文档的完整修订、来源范围、历史去向和使用方式。它不是第三份产品规则或执行计划。

# 1. 应使用的两份完整文档

| 文件 | 职责 | 建议替换位置 |
| --- | --- | --- |
| [WISELINK_R10_CURRENT.md](WISELINK_R10_CURRENT.md) | 现行产品、业务语义和技术规则；保留第1—14节整体组织 | 仓库 `docs/WISELINK_R10_CURRENT.md` |
| [WISELINK_R10_EXECUTION_PLAN.md](WISELINK_R10_EXECUTION_PLAN.md) | 当前批次、分工、集成、验证和后续主线 | 仓库 `docs/WISELINK_R10_EXECUTION_PLAN.md` |

这两份是可以直接阅读和使用的完整文本，不是修订条目、补丁片段或需要手工拼接的附录。Markdown为可编辑主文件；HTML是同一内容生成的阅读版，不是另一份设计。

# 2. 修订基础与证据边界

## 2.1 本次主要输入

1. 用户上传的 `WISELINK_R10_CURRENT.md`：含9月15日置顶新规则、旧R10正文、此前云文档镜像说明及运行片段。
2. 用户上传的 `WISELINK_R10_EXECUTION_PLAN.md`：含9月15日新执行顺序及较长历史记录。
3. 本对话中用户已明确的有效工程依据、事项参考、低负担交互、原文分析、正文保存、分流和开发期清理要求，以及上一轮针对两文件的一致性评审。
4. 对话已提供的JobAid方法、翻译重设计、Wiki/知识复用、F1—F6纠偏及用户价值说明，用于保留原文已有或此前明确的有效设计，不用于宣布当前源码和平台已实现。

没有读取用户本机仓库、Codex私有会话，也没有在本轮重新查询当前Git、线上数据库、Hosted安装、飞书云文档或真实工程结果。文中平台接口只保留材料中的职责与定位线索；未新增声称已存在的精确API、配额、重试开关或性能保证。

## 2.2 本次采用的处理原则

有效工程依据与参考边界采用用户最新确认。原文有价值的身份、授权、来源、语义、适用性、JobAid、连续工作、存储及用户阅读要求继续保留。此前“只许显式更新”“翻译优先”“收尾一次输出”“强制长期兼容”等已被新要求替代的执行描述，不再与新规则并列。

两份原件逐字节保留，历史事实不被改成新成功。最新工作15、同会话工作13/14以及部署/待部署线索仅作为最近交接，不冒充本轮运行证据。实际当前状态继续交由既有 `docs/coordination/M_INTEGRATION.md` 维护，本包没有创建一份替代它的伪实时文件。

JobAid原文中可定位的规定仍按其原作用表达。例如，JA-DS093 §5.3.1第9项对应所提供PDF第9页；相关修订说明、表单和案例用于理解方法。本文不替代受控程序，不宣称本轮已取得所有现行程序或缺失的附件5。

# 3. 主要修订

| 修订项 | 本次完整文本中的处理 |
| --- | --- |
| 新规则只置顶、正文仍发出旧指令 | 全文重写为同一套规则；旧正文和运行记录不再附在当前章节后 |
| 云文档镜像与本地设计混杂 | 新文本明确为本地修订稿；旧镜像声明只在原材料归档中保留 |
| 文件修订与评估更正混用 | 分开受控源文件、对文件的评估、事项认识、正式决定 |
| 最新有效与系统最新处理混用 | 正式版次、DocumentVersion、元数据、parseRun、semanticRevision、workRevision分别定义 |
| 新版分析忽略自身修订说明 | 明确自身修订说明→新版正文→以前评估及关联工程文件→新覆盖/综合 |
| 所有更新都依赖人工按钮 | 增加已授权并启用的真实变化入口；普通聊天和任意检索命中不无条件触发 |
| 翻译阻塞原文分析 | 后台工程依据只用原文/原始记录；Translation V2独立辅助阅读，不要求工程师逐块确认 |
| 最终结构化返回被写成唯一保存点 | 连续上下文中多次交付完整问题组；收尾只做必要综合和结束 |
| 定点更正再膨胀成大报告 | 先定位输入/生成/保存/读取问题，只扩实际必要字段，沿同一Host保存服务 |
| 更正完成但Overall仍展示旧结论 | 第一批同步更新相关综合，未完成时准确标明依据版本与覆盖限制 |
| Wiki被缩成原文单元搜索 | 保留原文、完整工程工作及问题索引三类职责，三类跳转与跨事项根来源复用 |
| OpenClaw退避成为全部业务状态 | Host管任务，原生job可继续唤醒；单任务/系统故障分开，无任务正常IDLE |
| CodeM恢复阻塞主线 | 有边界恢复原入口，失败则M或既有脚本接最小工作，不扩建控制面 |
| 前端仍被写作未开工 | F继续独立重构，M统一读取语义和发布组合；不接管视觉，不整边覆盖 |
| 兼容窗口成为统一前置 | 开发期按实际依赖协调切换，允许清理无用测试和旧协议，保留安全及有效资产 |
| F1—F6与H0/H1被重新清零 | 差异核对并继承已存在证据，H2重点变化/恢复，不新增总gate |

# 4. 原CURRENT章节与新章节对应

| 原章节 | 新文本去向 |
| --- | --- |
| 置顶9月15日规则及镜像信息 | 文档首页、§1—3；原镜像信息保留在历史 |
| §1 30秒结论 | §1产品目标与价值 |
| §2 开发范围 | §2范围、§9主流程、§11开发边界 |
| §3版本与继承 | §3完整版本术语、§11发布与清理 |
| §4组件职责 | §4职责与云端边界 |
| §5 Gap与成熟度 | §5 JobAid、适用性、风险和阶段性完成 |
| §6原生Review | §6调查/更正/保存/调度、§9交互、§11发布 |
| §7多轴语义 | §7原文与关联；保留稀疏表达和各轴独立 |
| §8引用、RAG、图谱 | §8知识复用与Wiki、§7实际引用 |
| §9初评与Review | §9初评、自身换版、参考变化、交互、Overall与正式采用 |
| §10数据、上下文与性能 | §10完整保留并按现行职责重组 |
| §11旧实施顺序与前端 | 实施任务移至执行计划；稳定规则在§11和§10 |
| §12运行摘要和历史 | §12只保留维护规则；旧事实全文保存在原件归档 |
| §13 No-Go | §13安全与工程边界，不保留过时实现冻结 |
| §14完成定义 | §14用户结果、持续知识和与风险相称的验证 |

# 5. 原EXECUTION_PLAN的处理

9月15日顶部主线保留并展开为可直接执行的计划。第一批拆清内容、综合读取与运行三条工作面，可以并行；后续仍按文件自身换版、知识复用和后台来源/长SB推进。

F1—F6沿用编号与业务含义，增加“先核对当前代码，不把历史缺陷当现存”的明确说明。H0/H1/H2保留，但不作为每次重新闯关。M/Luna/C/P/F的具体任务、CodeM恢复退出条件、交付回执、技术发布与开发期清理集中表达。

9月13日及更早的安装、模型、旧cron、旧来源失败、旧下一步与原手工授权记录，全部保留在原件归档，不再作为当前主线的同级标题出现。没有删除历史事实，也没有将历史原文改写成符合当前结论的新记录。

# 6. 原件与历史保存

仓库替换前两份文档保留于 Git 提交 `35224188b46b078e0f5570c00ddef26e7ba285bd` 的 `docs/WISELINK_R10_CURRENT.md` 与 `docs/WISELINK_R10_EXECUTION_PLAN.md`。本次仓库执行只取得两份修订正文与本说明，未取得包内上传原件归档，因此不声称已复制或校验上传原件。现行正文不再承载旧运行指令。

旧镜像来源、日期、运行事实与历史授权保留在该提交中；不冒充新的云端同步。未另行复制私密运行归档或发布原件。

# 7. 放入项目的方式

只替换仓库中的两份同名Markdown，不覆盖整个 `docs` 目录。将本说明及经审查可保留的历史文件放到对应位置；若仓库已有合适历史位置，可以调整归档链接，不需要另建文档平台。

运行记录 `docs/coordination/M_INTEGRATION.md`、F/P实际工作树、源码和现行安装包未包含在本包中。本包对这些位置的引用是项目已有入口，不是宣称文件已复制到交付包。

HTML目录仅供阅读；不要把HTML或样本入口作为生产应用代码部署。飞书云文档后续由有权限的执行者按接受范围更新，并记录实际读回revision；本次没有写入。

完成替换后，M只需核对生成调用、保存和阅读中是否还引用旧相反规则，并按执行计划第一批推进。无需再开一轮架构评审或将历史原件全部灌入每次任务。

# 8. 本包检查范围

交付前检查两份正文完整存在、Markdown代码围栏和表格结构、相互链接、HTML页面及原件复制一致性。这些是文档交付检查，不是仓库lint、数据库测试、模型运行或产品验收。

本次未发起Git提交/推送、云文档修订、安装/发布、CodeM任务、后台扫描或工程工作生成。后续动作由现有项目流程和实际授权执行。
